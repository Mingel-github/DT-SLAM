# DT-SLAM 运动一致性分组方向：批判性审阅与下一阶段冻结

日期：2026-07-30
状态：研究路线决策；本轮不实现新算法，不解锁 G1-F/G1-D
本地代码：`/home/zhu/dynaslam_ws/DT-SLAM`

---

# 一、结论先行

外部意见的主要方向值得吸收：

> 当前最强的类别无关信号是 sparse ego-flow continuous residual；F2/F3/F4
> 暴露的核心缺口，是如何把局部运动异常组织成可靠、可验证的运动一致观测组，
> 而不是继续寻找单点阈值或简单深度传播规则。

但需做四项关键修正：

1. 当前项目需要的首先是**运动一致观测组**，不一定是具有长期身份的“对象”；
2. 刚体上的二维残余光流一般不满足
   `v_A ≈ v_B ≈ v_C`，正确约束应是“对同一运动模型一致”；
3. DetectFusion 的未知动态分支不要求 YOLO 框；它使用静态地图 ICP residual
   与几何 segment 相交；
4. F3 失败不能简单归因于“只用了两帧”。Dai 原方法的前端也使用相邻两帧，
   当前 F3 缺少的是原方法中的完整三维相对位置状态、协方差、迭代离群剔除和
   连通分组，而不只是帧数。

因此，当前不直接命名或实现新的 F5。下一项工作冻结为：

> **运动一致性分组的文献与输入可行性审计。**

审计通过后，才决定是否值得实现一个轻量 sparse grouping shadow。

---

# 二、先修正问题定义

## 2.1 当前语义分支的准确能力

不能写成：

```text
人、车等已知动态已经由 YOLO 解决。
```

当前本地 `YOLOSegment.cc` 只读取：

```cpp
float score = detMat.at<float>(i, 4); // class 0 = person
```

并且头文件和实现均明确是 `person` mask。因此当前事实是：

```text
已实现：
  person 类的像素级语义候选过滤

未实现：
  car/truck/cart 等其他类别过滤
  “是否正在运动”的语义内运动判断
```

YOLO 输出表达的是：

> 已知类别的潜在动态区域。

它不等于：

> 已确认正在独立运动的区域。

## 2.2 类别无关分支的目标

当前更准确的目标为：

> 在排除 person 语义区域后，根据 RGB-D 和初始相机运动，寻找不符合静态场景
> 模型、且在空间和时间上具有一致性的观测集合。

第一阶段输出可以是：

```text
motion-coherent feature group + confidence
```

不要求立即输出：

```text
对象类别
对象身份
对象长期轨迹
对象位姿
完整像素 mask
```

“对象候选”只能是经过验证后的解释，不能由一个 cluster 标签自动推出。

---

# 三、对当前几何路线的重新评价

| 已实验路线                      | 实际得到的结论                        | 不能推出的结论         |
| -------------------------- | ------------------------------ | --------------- |
| G0/G2 dense depth residual | 能测量跨帧深度不一致、遮挡和新前景证据            | 不能直接等同对象运动      |
| G0-3 flood fill            | all-seed 局部深度连续传播会链式淹没大区域      | 不能否定所有区域方法      |
| GJ Ji cluster baseline     | 文献基线已完成，当前 cluster error 可分性不足 | 不能否定所有聚类/区域评分   |
| G2-1 multi-reference dense | 覆盖提升，但成本和可分性不足                 | 不能证明多参考原则错误     |
| G2-4F1 sparse ego-flow     | 未知气球粗代理内连续 residual 稳定富集       | 不能直接形成动态二值判断    |
| F2 hard candidate          | strict holdout 安全性/灵敏度条件失败     | 不能否定 F1 连续信号    |
| F3/F3U local rigidity      | 局部边测量可计算，但未形成可靠判决              | 不能否定完整点关联或多运动分割 |
| F4 simple depth component  | 整区聚合会在部分帧稀释 F1 信号              | 不能否定更强几何分段      |

目前可冻结的科学结论是：

> 低层运动证据存在；当前失败集中在从证据到可靠判决或可靠空间支持的转换。

但“对象级 grouping 是唯一缺口”仍然过强。一个足够精确的 feature-level
fail-safe 理论上也可用于 G1-F，只是当前 F2 尚未达到该条件。

---

# 四、为什么 depth residual 不是对象检测器

当前有符号残差：

\[
r_z = D_{\mathrm{pred}} - D_{\mathrm{cur}}
\]

回答的是：

```text
当前观测是否符合由历史表面和相机位姿预测的静态几何？
```

相同的正残差可能来自：

- 新的运动前景进入；
- 遮挡关系变化；
- 深度边界混合像素；
- 位姿误差；
- 深度噪声或空洞；
- 坐标域或时间同步误差。

因此更准确的身份是：

```text
scene-inconsistency / occlusion evidence
```

而不是：

```text
object-motion label
```

这支持保留 depth residual 作为辅助证据或风险字段，但不支持重新调 flood fill
阈值来强行恢复对象。

---

# 五、为什么不能使用 `v_A ≈ v_B ≈ v_C`

同一刚体上的多个三维点共享同一个刚体变换：

\[
\mathbf X_{i,t} \approx
\mathbf T^{\mathrm{obj}}_{t\leftarrow r}\mathbf X_{i,r}.
\]

但投影后的二维运动：

\[
\Delta \mathbf u_i =
\pi(\mathbf X_{i,t})-\pi(\mathbf X_{i,r})
\]

取决于：

- 点的深度；
- 图像位置；
- 物体平移方向；
- 物体旋转；
- 相机运动；
- 透视投影。

所以同一刚体的不同点通常不具有相等二维光流。合理的一致性形式包括：

1. 对同一 SE(3) 物体运动假设具有小残差；
2. 三维点间相对位置在噪声范围内保持；
3. 在有依据的局部近似下，残余运动方向或仿射模型相容；
4. 使用带测量不确定性的 pairwise motion affinity。

“相似 flow vector”最多是小区域、弱旋转、相近深度下的近似 `[H]`，不能作为
冻结方法公式。

---

# 六、文献能支持什么

## 6.1 FlowFusion `[L/A]`

原方法证明 optical-flow residual 可作为类别无关动态证据，但其计算单元不是
稀疏 ORB feature group。FlowFusion：

- 估计 dense optical flow；
- 对 RGB-D 点云做 supervoxel 过分割；
- 聚合 intensity/depth/flow residual；
- 联合更新动态分割与相机运动。

可借用：

```text
observed flow - ego-motion-induced flow
```

不能直接声称：

```text
FlowFusion 已证明当前 sparse ORB grouping 有效。
```

## 6.2 Jaimez et al. 2017 `[L]`

该方法：

- 用三维 K-means 形成几何 cluster；
- 联合估计相机运动和 piecewise-rigid scene flow；
- 将 cluster 分为静态和运动部分；
- 在 QVGA、多核 CPU 上平均约 80 ms。

它支持“区域共享运动模型”的物理合理性，但不支持当前 30 FPS 约束下直接移植。

## 6.3 Dai et al. point correlations `[L/A]`

原方法：

- 用 Delaunay 图连接地图点；
- 以三维相对位置为 edge state；
- 传播 RGB-D 测量协方差；
- 用马氏残差、鲁棒优化和离群边剔除；
- 对剩余图做 connected components；
- 假设最大空间体积的 component 为可靠静态组。

重要修正：

- Dai 前端确实只用当前/上一帧检查边；
- 后端才维护长期 point-correlation graph；
- 当前 F3 只实现了局部两帧 edge strain 测量，没有复现原分组算法。

因此 F3 的负结果不能归结为“两帧天然不够”，也不能用来否定 Dai 完整方法。

## 6.4 ClusterSLAM `[L]`

ClusterSLAM 使用：

- landmark pairwise noise-aware motion affinity；
- agglomerative clustering；
- 短 frame chunks 和 consensus clustering；
- decoupled factor-graph optimization；
- cluster assignment 与运动估计迭代更新。

它直接支持“landmark motion affinity 可恢复刚体组”，但论文明确是一个 SLAM
backend。直接复制会违反当前：

```text
不改后端
不引入对象运动优化
保持轻量前端
```

只能借鉴 affinity 的问题形式，不能称为可直接接入方案。

## 6.5 长轨迹 RGB-D motion segmentation `[L]`

Bertholet et al. 使用 RGB feature tracks 和深度进行初始化聚类，然后在整个
RGB-D 序列上联合优化对象分割和刚体运动。

它证明：

```text
feature trajectories → motion segmentation
```

有成熟研究基础。

但它是全序列、全局能量方法，文中处理规模约数十到两百帧，不是 30 FPS
ORB-SLAM2 前端模块。

## 6.6 DetectFusion `[L]`

需要修正外部意见中的误解：

```text
known object:
  YOLO box + 3D geometric segment

unknown moving object:
  static-map ICP residual mask + geometric segment
```

未知动态分支不依赖未知对象的 YOLO proposal。真正不适合直接搬入当前项目的
原因是：

- 需要 dense surfel static map；
- 需要 frame-to-model ICP residual；
- 维护对象/静态多地图；
- 完整系统约 22 FPS，不满足当前 30 FPS 目标。

## 6.7 StaticFusion 与 SInDSLAM `[L]`

StaticFusion 联合估计 camera motion 与 probabilistic static/dynamic
segmentation，并做 dense weighted fusion；它不是轻量独立前处理器。

SInDSLAM 使用完整的：

- 3D K-means；
- depth gradient、plane edge；
- RAG/depth-histogram re-clustering；
- dense optical-flow residual；
- PROSAC、双阈值和时序先验。

F4 的简单 depth connected component 不是 SInDSLAM 的充分复现，F4 失败只
否定当前简化容器。

---

# 七、选择性吸收结果

## 7.1 接受

1. 保留 F1 continuous sparse ego-flow residual；
2. 停止继续调整 flood fill、`q`、region area 和简单 depth component；
3. 不把 F1 直接阈值成 feature 删除；
4. 研究空间—时间运动一致性分组的文献和可实现性；
5. F3 失败不否定完整 motion grouping；
6. 下一轮先做研究决策，不抢先实现新模块；
7. G1-F/G1-D 继续锁定。

## 7.2 修正后接受

| 外部表述                                | 修正后的表述                                                 |
| ----------------------------------- | ------------------------------------------------------ |
| 类别无关动态物体发现                          | 类别无关的运动一致观测发现；对象解释需后验验证                                |
| feature residual → object candidate | feature residual → motion-coherent group → 可选对象候选      |
| 同一物体 `v_i` 相近                       | 同一刚体对同一运动模型一致                                          |
| 两帧 rigidity 失败因为帧数少                 | 当前简化缺少完整状态、协方差、分组和验证；多帧只是候选增强                          |
| YOLO 已解决人和车                         | 当前代码只生成 person mask，且是潜在动态语义而非真实运动                     |
| DetectFusion 未知箱子缺少 proposal        | 未知分支无需 YOLO proposal，但依赖 dense static-map ICP residual |

## 7.3 暂不接受

1. “对象级 grouping 是进入 G1-F 的必要条件”——尚未证明；
2. “feature motion grouping 已经是最合适最终路线”——只有方向性证据；
3. “增加多帧就能修复 F3”——没有依据；
4. “motion cluster 等于物体实例”——一般不成立；
5. “完整对象 mask 可由 sparse grouping 自然得到”——低纹理箱子仍是根本缺口；
6. 在没有输入支持量、轨迹寿命和运行预算审计前指定具体聚类算法。

---

# 八、下一项工作的冻结范围

下一项工作只做：

> **运动一致性分组文献与输入可行性审计。**

不做算法实现，不选择 hard threshold，不进入 tracking mutation。

## 8.1 必须回答的问题

1. 当前 C++ 是否有跨 2/3/5 帧稳定 feature/MapPoint identity；
2. 未知动态代理区域内每帧有多少可用非语义 feature；
3. 这些 feature 的 track survival 是否足以估计共同运动；
4. 两帧三维 point-correlation、短轨迹 affinity、几何 cluster scene flow
   三者中，哪一类最小改造且不需要后端；
5. 如何显式处理深度边界、遮挡、无效深度和 pose uncertainty；
6. 如何避免“最大 component 必为静态”的脆弱假设；
7. 输出是否只需 `D_feat + confidence`，还是必须同时产生 `M_depth`；
8. 额外计算是否还有真实的 30 FPS 预算。

## 8.2 候选方法的初始工程筛选

| 候选                              | 文献支持             | 与当前接口              | 主要风险                     | 当前身份           |
| ------------------------------- | ---------------- | ------------------ | ------------------------ | -------------- |
| Dai 前端 point-correlation 完整子集   | 强                | 较接近 RGB-D MapPoint | 分组假设、协方差、图迭代             | 优先审计           |
| 短窗口 noise-aware motion affinity | ClusterSLAM 支持原理 | 需稳定 landmark track | 原文依赖 backend，复杂度较高       | 仅审计 adaptation |
| 长轨迹 motion segmentation         | Bertholet 等支持    | 需要多帧轨迹             | 延迟、遮挡、非实时                | 理论参考           |
| geometric-cluster scene flow    | Jaimez 支持        | 输出区域较自然            | 约 80 ms/QVGA、重型          | 不直接实现          |
| FlowFusion supervoxel residual  | FlowFusion 支持    | 可输出区域              | dense flow/VO/cluster 耦合 | 不直接实现          |
| DetectFusion unknown branch     | DetectFusion 支持  | 可输出完整 segment      | 需要 dense map/ICP，多地图     | 超出当前范围         |

## 8.3 放行条件

只有同时满足以下条件，才讨论新的 grouping shadow：

- 原始论文支持核心一致性表达；
- 当前数据确有足够 feature/track 支持；
- 不要求修改 Optimizer、g2o 或后端；
- 不增加第三次 PoseOptimization；
- 能在 shadow 中输出连续 confidence 和 unknown 状态；
- 有独立静态风险检查；
- 预计成本符合剩余实时预算；
- 不把 proxy 当 GT，也不在已打开 holdout 上调参。

若这些条件不满足，应明确停止 sparse grouping 路线，而不是继续发明阈值。

---

# 九、当前总计划位置

```text
Semantic baseline                         已冻结
G0/G2 depth geometry evidence             已完成大量测量与负实验
GJ Ji literature baseline                 已完成，未成为主路线
G2-4F1 sparse ego-flow evidence           当前最强连续信号
F2/F3/F3U/F4 decision/grouping attempts   未形成可靠判决

当前：
  运动一致性分组的文献与输入可行性审计

尚未：
  新 grouping shadow
  G1-F 真实特征过滤
  G1-D 深度/地图过滤
  geometry ATE/RPE 改善验证
```

所以总体研究方向没有推翻，但下一步从“feature temporal persistence +
fail-safe”修正为：

> 先判断轻量 motion-coherent grouping 是否有足够文献、输入支持和实时可行性；
> 通过后再定义具体实现。

---

# 十、参考资料

本地优先：

```text
/home/zhu/Desktop/paper_notes/detectfusion.md
/home/zhu/Desktop/paper_notes/SInDSLAM.md
/home/zhu/Desktop/papers/2019_DetectFusion_Known_Unknown_Dynamic_Objects.pdf
/home/zhu/Desktop/papers/
  Qi 等 - 2025 - Semantic-Independent Dynamic SLAM Based on
  Geometric Re-Clustering and Optical Flow Residuals.pdf
/home/zhu/dynaslam_ws/results/g2_4f3u_2026-07-30/literature/
  Dai_2022_Point_Correlations_arXiv1811.03217.pdf
```

原始论文链接：

- FlowFusion: <https://arxiv.org/abs/2003.05102>
- Jaimez et al. 2017: <https://cvai.cit.tum.de/_media/spezial/bib/jaimez_et_al_vosf_2017.pdf>
- Dai et al.: <https://arxiv.org/abs/1811.03217>
- ClusterSLAM: <https://openaccess.thecvf.com/content_ICCV_2019/html/Huang_ClusterSLAM_A_SLAM_Backend_for_Simultaneous_Rigid_Body_Clustering_and_ICCV_2019_paper.html>
- DetectFusion: <https://arxiv.org/abs/1907.09127>
- StaticFusion: <https://ora.ox.ac.uk/objects/uuid%3Ab06224b2-62dd-459f-bbf7-1352dbbb5dc8>
- Temporally Consistent Motion Segmentation from RGB-D Video:
  <https://arxiv.org/abs/1608.04642>

---

# 十一、输入审计完成后的覆盖更新

原冻结项“运动一致性分组文献与输入可行性审计”已经完成。

关键事实：

```text
代理内 MapPoint support:
  balloon 10.53%
  balloon2 3.28%

三图像仍有 >=3 tracks:
  4/8、4/9 frames

六图像仍有 >=3 tracks:
  2/8、2/9 frames
```

这否决了：

```text
fixed 3/5-frame persistence gate
existing MapPoint long-track clustering as universal route
track lifetime as dynamic score
```

但没有否定两帧 motion grouping 的学术合理性。

Dai 原文进一步核对表明，其两帧前端仍依赖 tracked MapPoint、三维 edge state、
RGB-D covariance、point-correlation optimization、Mahalanobis/chi-square
edge culling、connected components 和最大体积静态组。把节点替换成 transient
LK feature 是显著 `[A/H]`，不能作为当前小修补。

由于用户已放宽 30 FPS 硬约束，且最终目标同时包含 `D_feat` 与 `M_depth`，
下一项改为：

> **G2-4R1 normal+distance geometric-segment representation shadow。**

依据为 Tateno frame-wise depth segmentation，以及 DetectFusion 将该类
geometric segment 用作 motion residual 的区域支持。第一版只审计 vertex、
normal、concavity/distance edges 和 connected components；不实现 Tateno
全局增量地图，也不实现 DetectFusion surfel/ICP motion segmentation。

详细决策和 SPEC：

```text
results/g2_4_motion_grouping_2026-07-30/
  G2_4_MOTION_GROUPING_ROUTE_DECISION.md
  G2_4R1_NORMAL_DISTANCE_SEGMENTATION_SHADOW_SPEC.md
```

G1-F/G1-D 仍锁定。
