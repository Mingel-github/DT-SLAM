# DT-SLAM 成果收束验证记录

日期：2026-08-07

范围：只验证冻结代码、工具和最小运行入口，不重新运行长序列几何，不调整算法参数。

## 1. 构建

实际有效构建入口：

```bash
cd /home/zhu/dynaslam_ws/DT-SLAM
make -C build rgbd_tum \
  sin_style_region_dynamic_parity_test \
  sin_style_dense_flow_native_parity_test \
  sin_style_depth_filter_test -j2
```

结果：四个目标全部构建通过。

说明：直接在`DT-SLAM/`运行`make rgbd_tum`会失败，因为真正的生成式Makefile位于`DT-SLAM/build/`。最终复现索引已修正该歧义。

## 2. 自动检查

| 检查 | 结果 |
| --- | --- |
| `sin_style_depth_filter_test` | PASS，打印`SIn-style mapping depth filter tests passed` |
| `offline_gazebo_occupancy_audit --self-test` | PASS，hit/miss能够先占用再由free射线清除 |
| Python `py_compile` | PASS：正式runner、点云导出、Gazebo mask审计和AWS仿真工具 |
| `git diff --check` | PASS |
| `sha256sum -c` | PASS：16个冻结配置、工具、仿真世界、模型和词袋文件 |

区域判决与dense-flow parity二进制本轮完成构建，但没有重新生成或拼装作者reference replay输入；完整29帧parity继续引用`results/sindslam_s2_2026-08-04/S2_REGION_FEATURE_FILTER_RESULT.md`中已经通过的结果，不伪装成本轮重新执行。

## 3. 10帧纯ORB smoke

输入：AWS Small House正式序列前10个association；基础`GAZEBO.yaml`；Viewer关闭；独立`/tmp`工作目录，未覆盖正式轨迹。

结果：

```text
processed frames       10/10
new map points         990
deadline missed        0/10
actual FPS             19.9624
trajectory save        PASS
```

日志确认`Geometry Shadow: disabled`和`Ji Geometry GJ-1 Shadow: disabled`，说明默认基础配置没有意外启动legacy几何。

## 4. 10帧同位姿点云导出 smoke

使用AWS完整组合模式轨迹和已导出的S3 mask，最大输入10帧、像素步长16：

```text
matched frames         10
missing masks          0
unmatched poses        0
unfiltered points      10850
filtered points        10131
removed points         719
removed fraction       6.6267%
```

这只验证`--mask-pattern`、`--dynamic-threshold`和同位姿点云写出接口，没有形成新的算法效果结论。

## 5. Viewer检查

`view_mapping_comparison.cc`通过独立PCL CMake构建，并成功同时加载AWS四份点云：50,254、47,664、36,543和31,724个点/体素，进入交互事件循环。GUI目视属于辅助检查，不代替机器可读Mapping指标。

## 6. 结论

当前冻结版本满足成果收束的最小工程条件：主程序可构建，默认基础模式可运行，S3过滤和离线概率Mapping自检通过，点云导出与Viewer入口可用，冻结文件哈希一致。长序列数值继续使用已冻结的权威运行结果，不因收束重复计算。
