# DT-SLAM 几何动态模块阶段进度

更新日期：2026-07-31

## 1. 总体目标

当前研究路线为：

```text
ORB-SLAM2 RGB-D
+ YOLOv8-seg 语义分支
+ 类别无关几何分支
→ 动态特征与动态深度区域剔除
```

目标是减少：

1. 动态特征参与位姿估计造成的静态背景错位；
2. 动态物体特征或深度写入静态地图造成的残影和拖影。

当前不做：

- 对象级轨迹；
- 对象级 BA；
- g2o 代价函数修改；
- 动态对象地图；
- TSDF、OctoMap 或 3DGS；
- 动态 SLAM 后端重构。

## 2. 总体阶段进度

| 阶段 | 原计划内容 | 当前状态 | 实际效果 |
| --- | --- | --- | --- |
| Semantic baseline | 修复 YOLOv8-seg mask、GPU推理和动态特征处理 | 已完成并冻结 | 同步语义版本短程测试基本接近30 FPS；async暂缓 |
| G0-1 | 单参考帧 depth warp、z-buffer、signed residual | 已完成 | 坐标方向和残差符号正确；单套geometry通常约2–4 ms |
| G0-2 | valid/consistent/positive/negative/unknown分离 | 已完成 | 无深度、无投影覆盖不再被解释为静态 |
| G0-2C | RGB、depth、mask坐标域及畸变检查 | 已完成 | TUM1/TUM3的几何相机模型和坐标域已经明确 |
| G0-2P | SLAM pose与GT pose敏感性对照 | 已完成 | GT pose没有明显消除背景seed，位姿不是主要单因 |
| G0-2A-static | fr1/xyz静态负样本审计 | 已完成 | 正seed均值约0.521%；86.20%位于深度边缘2像素内 |
| G0-2A-dynamic | 动态区域seed质量审计 | 已完成 | 自动person代理审计完成；像素级proxy precision约4.26% |
| G0-3 | 固定阈值all-seed区域生长 | 已实验并判定失败 | region覆盖有效区域约95%–97%，发生链式吞并 |
| G0-3R | 区域支持率修补 | 已实验但未解决 | 没有解决大型深度连通区域传播问题 |
| G0-4F | ORB feature-level shadow evidence | 已完成但未通过门控 | 特征中心proxy precision约7.09%，不足以进入实际过滤 |
| G0-3B/C | DynaSLAM/ReFusion传播规则对照 | 暂缓 | 当前direct seed质量不足，不宜直接比较传播规则 |
| G1-F0 | 真实接入前 MapPoint counterfactual | 已完成 | 旧 `0.20%` 静态误删预算过严；按用户新的工程容忍原则不再作为绝对阻断条件 |
| G1-F1 | 稀疏 ego-flow tracking association 过滤 | 已实现并完成首轮正式评价，默认关闭 | fr1/xyz 最大约1.7%退化；semantic+geometry walking 三轮 ATE 中位改善8.9%–15.7%，FPS代价约0.5% |
| G1-M0 | q10 MapPoint 写入 counterfactual | 已完成，只读 | walking/balloon 每轮中位约653/202个候选会创建 MapPoint；确认 tracking 删除后仍会重建 |
| G1-M1 | 几何候选 MapPoint 写入保护 | 已实现并完成四序列评价，默认关闭 | applied 行 candidate MapPoint 创建为0；相对 control 的 ATE/RPE 中位变化均约3%以内 |
| G1-D | 动态深度区域过滤 | 未开始 | 尚无可靠完整区域mask |
| GJ-1 | Ji 2021三维深度K-means区域生成 | 已完成 | 工程/数据验收通过；约58 ms，30 Hz性能门控失败 |
| GJ-2 | ORB到cluster映射与cluster重投影误差统计 | 已完成 | 工程链路通过；walking与静态误差重叠 |
| GJ-2A | cluster误差与离线person proxy自动可分性审计 | 已完成，排序门控通过 | identity-rho Top-3相对随机约2.14×；仍未批准绝对阈值和过滤 |
| GJ-3P | Ji动态阈值、rho和支持数来源审计 | 已完成 | 官方PDF和arXiv源码均未公开完整判定；严格复现不可补齐 |
| GJ-3A | 透明的相对阈值适配shadow | 已完成但失败 | λ=0.5最大化proxy F1却在静态序列选择约50%区域，不进入过滤 |
| G2-1 | 最近5关键帧多参考稠密证据计数shadow | 已完成，含geometry-only和在线同步语义闭环 | geometry-only proxy precision约34%–40%；在线语义改变参考覆盖域；在线G2-1约14–19 ms、端到端约19–21 FPS，不进入过滤 |
| G2-2R | ORB-SLAM2共视参考选择shadow | 已完成 | K=1/2/5选择、缺失参考和age审计完成；共视不保证时间新鲜，单改选择策略不能达到30 FPS |
| G2-2S | DynaSLAM启发的ORB特征关联深度采样shadow | 已完成 | K=5几何降至约0.82–1.09 ms，但五参考整图覆盖仅0.47%–0.91%，保留为稀疏证据baseline |
| G2-4F0 | ORB feature 中心 direct multi-reference evidence | 已完成但未过门 | 无人物箱体内 positive evidence 未相对背景富集 |
| G2-4F1 | 稀疏 LK observed-flow 减 RGB-D/SE(3) ego-flow | 已完成 shadow 实现与同步语义开发审计 | F1 约2.5–2.7 ms；moving-box 主门不可评价，F1D 气球数据提供明确方向性 evidence |
| G2-4F1D | 非 holdout 气球开发数据门与连续 evidence | 已完成 | 6 个可测 exact-zero person-overlap 帧中框内/背景中位 `11.919/0.723 px`；未选阈值，G1仍锁定 |
| Failure-driven | 光流、刚性图、自由空间 | 稀疏光流已执行；刚性图/自由空间未开始 | 只按具体失败类型推进，不堆叠模块 |

## 3. 已完成的语义基线

语义baseline已经完成清理和冻结，主要包括：

- 修复YOLOv8-seg proto/mask后处理；
- 统一语义mask尺寸、类型和极性；
- 修复语义动态特征在Tracking和地图写入中的处理；
- CUDA Execution Provider运行验证；
- 端到端计时；
- 同步语义模式性能优化；
- baseline/semantic短程对照。

当前同步语义已经基本达到阶段目标，因此async架构暂缓。后续几何模块不得修改
YOLO推理逻辑，语义baseline作为独立对照保留。

## 4. G0几何证据链

### 4.1 G0-1：单参考帧深度warp

已实现：

```text
上一成功跟踪帧深度
→ 反投影
→ 使用相对位姿变换到当前帧
→ forward projection
→ z-buffer
→ predicted depth
→ signed depth residual
```

残差定义：

```text
r_z = predicted_depth - current_depth
```

解释：

- `r_z > threshold`：当前观测比预测静态表面更近，作为positive seed；
- `r_z < -threshold`：当前观测更远，作为negative/disocclusion诊断；
- 未投影、无效深度和无比较区域：unknown；
- 小残差有效比较：consistent evidence。

该实现是受DynaSLAM多视图深度一致性启发的单参考帧稠密改造，不是DynaSLAM
geometry reproduction。

### 4.2 G0-2：有效性建模

当前几何输出明确分离：

```text
valid comparison
consistent evidence
positive seed
negative diagnostic
unknown/no evidence
```

该设计避免将无效深度、未覆盖区域和遮挡空洞错误解释为静态证据。

### 4.3 G0-2C：坐标域和畸变

已经明确：

- TUM3使用零畸变针孔模型；
- TUM1 Tracking保留原生非零畸变RGB标定；
- TUM1 raw registered depth geometry使用TUM推荐的零畸变
  `fx=fy=525, cx=319.5, cy=239.5`；
- semantic mask与`Frame::mvKeys`位于原始RGB像素域；
- Optimizer主要使用`Frame::mvKeysUn`。

Bonn具有非零畸变，在没有统一RGB/depth/mask坐标域前，不能直接使用当前raw
pinhole warp评价几何效果。

### 4.4 G0-2P：SLAM pose与GT pose

在相同深度数据上同时使用：

```text
ORB-SLAM2当前位姿
数据集GT位姿
```

结果显示GT pose没有系统性降低positive seed。当前背景seed不能主要归因于
ORB-SLAM2位姿误差，但仍可能同时受到以下因素影响：

- 深度边界和空洞；
- RGB-depth时间差；
- 遮挡和显露；
- forward rasterization；
- 深度噪声；
- 标定近似。

## 5. G0-2A审计结果

### 5.1 静态负样本：TUM fr1/xyz

主要结果：

| 指标 | SLAM pose |
| --- | ---: |
| valid comparison coverage mean | 71.68% |
| absolute residual mean | 0.00964 m |
| positive seed ratio mean | 0.521% |
| positive seed ratio p95 | 1.601% |
| geometry mean | 3.48 ms |

27个抽样帧中：

| 距深度边缘距离 | positive seed占比 |
| --- | ---: |
| 2像素内 | 86.20% |
| 3像素内 | 93.53% |
| 5像素内 | 98.39% |

说明当前direct seed高度集中在深度跳变和无效深度边缘。

### 5.2 动态代理：TUM fr3/walking_xyz

为减少人工工作量，使用离线YOLOv8n-seg person mask作为动态区域代理标签。
该mask不是运动真值：

- person内部可能暂时静止；
- person外可能存在YOLO漏检或其他动态区域；
- 离线Ultralytics后处理与项目C++ YOLO路径不是逐像素等价。

SLAM pose加权结果：

| 指标 | 数值 |
| --- | ---: |
| person proxy有效比较覆盖 | 3.314% |
| positive seed落入person proxy | 4.260% |
| conditional recall | 14.519% |
| proxy外positive rate | 5.330% |

28个调试抽样帧中：

| 距深度边缘距离 | positive seed占比 |
| --- | ---: |
| 2像素内 | 95.43% |
| 3像素内 | 98.36% |
| 5像素内 | 99.66% |

结论：

> 单参考帧signed depth residual能够产生类别无关的局部运动和遮挡边界证据，
> 但目前不是完整动态对象mask。

## 6. G0-3/G0-3R失败实验

### G0-3

从所有positive seed出发，按相邻深度差小于固定阈值进行四邻域传播。

失败表现：

- region覆盖valid comparison约95%–97%；
- seed到region面积放大约27–31倍；
- walking和sitting场景的region统计明显重叠。

原因是：

```text
局部相邻深度连续
≠
属于同一个运动对象
```

墙面、地面、斜面和家具表面可以通过一系列小深度差形成巨大连通分量。少量错误
seed落入这些连通分量后，会触发整片区域传播。

### G0-3R

尝试加入区域positive support、面积和残差统计，但没有解决传播结构问题。

冻结结论：

```text
G0-3/G0-3R = 已完成的失败baseline
默认关闭
不得进入mvbDynamic
```

不再通过继续调整面积、seed ratio或固定阈值掩盖失败。

## 7. G0-4F特征级shadow结果

对`Frame::mvKeys`检查其中心或半径0、1、2、3像素邻域内是否存在positive seed。
该步骤只统计，不修改特征动态状态。

TUM walking结果：

| 半径 | person eligible coverage | proxy precision | conditional recall | proxy外候选率 |
| ---: | ---: | ---: | ---: | ---: |
| 0 px | 5.328% | 7.090% | 15.281% | 7.561% |
| 1 px | 7.947% | 6.738% | 21.922% | 15.693% |
| 2 px | 11.420% | 6.880% | 24.654% | 23.392% |
| 3 px | 15.391% | 7.276% | 26.389% | 30.235% |

TUM fr1/xyz静态候选率：

```text
r0=1.108%
r1=2.721%
r2=4.533%
r3=6.479%
```

扩大邻域提高了一些recall，但背景候选增长更快，precision没有改善。因此：

```text
G0-4F = 完成的负结果
G1-F  = 不进入
```

## 8. 相对初始计划的调整

### 8.1 人工标注改为自动代理审计

原计划考虑少量人工动态区域标注。实际改为：

- 离线person proxy mask；
- 自动precision/recall统计；
- 自动CSV、overlay和日志；
- 静态序列作为负样本对照。

这样减少了用户人工工作量，但最终论文级motion mask评价仍需要合适真值数据。

### 8.2 区域生长不再直接进入基础版本

原设想：

```text
positive seed → depth region growing → dynamic region
```

实验表明该组合不成立，因此region growing不再作为未经验证的默认步骤。

### 8.3 特征标记改为先做shadow门控

原计划可能直接给ORB特征设置几何动态状态。实际先完成G0-4F诊断，结果没有通过
precision门控，因此没有写入`mvbDynamic`。

### 8.4 DynaSLAM/ReFusion传播规则继续暂缓

两种文献传播方法都依赖更高质量的输入seed。当前seed本身定位能力不足，直接更换
传播函数只能形成错误输入上的局部消融，不能解决主要问题。

### 8.5 不增加第三次PoseOptimization

保持ORB-SLAM2已有优化路径，当前geometry只做shadow，没有修改`Optimizer.cc`
或无条件增加额外位姿优化。

## 9. 当前已经可靠完成的能力

1. 相邻成功帧深度投影；
2. z-buffer可见性处理；
3. signed depth residual；
4. positive、negative、consistent和unknown分离；
5. SLAM/GT pose双模式对照；
6. 静态与动态序列自动统计；
7. ORB特征级几何证据测量；
8. 坐标域、深度尺度和畸变风险检查；
9. geometry shadow运行时间统计；
10. 语义mask与几何证据的自动代理评价。

当前准确状态：

```text
几何证据测量框架已经完成；
第一种区域恢复方案已被实验否定；
第一种特征级直接过滤方案未通过门控；
真正的几何动态过滤尚未开始。
```

## 10. 尚未完成

- 未知动态箱子的可靠检测；
- 可靠完整的动态深度mask；
- geometry-only正式实验；
- 几何动态状态写入Frame；
- 几何动态MapPoint过滤；
- TrackLocalMap中的二次过滤；
- 新关键帧和MapPoint写入否决；
- 稠密点云动态深度过滤；
- 语义＋几何最终融合；
- Bonn运动箱子实验；
- Ji 2021作者未公开二值动态判定的严格复现；
- 轻量空间采样、MAD和短时序版本；
- 光流、刚性图或自由空间增强。

## 11. 性能边界

已完成的代表运行：

| 实验 | actual FPS | active total mean |
| --- | ---: | ---: |
| walking G0-4F，预计算语义mask | 28.558 | 27.367 ms |
| fr1/xyz G0-4F静态对照 | 29.541 | 30.002 ms |
| walking 在线CUDA语义＋G2-1 | 20.704 | 47.483 ms |
| sitting 在线CUDA语义＋G2-1 | 20.785 | 47.385 ms |
| fr1/xyz 在线CUDA语义＋G2-1 | 19.016 | 51.853 ms |
| walking 在线CUDA语义＋G2-2R K=1/2/5 | 25.497 / 24.173 / 21.135 | 37.964 / 40.247 / 46.432 ms |
| sitting 在线CUDA语义＋G2-2R K=1/2/5 | 25.881 / 24.275 / 21.016 | 37.675 / 40.331 / 46.851 ms |
| fr1/xyz 在线CUDA语义＋G2-2R K=1/2/5 | 24.781 / 22.875 / 19.063 | 39.502 / 42.955 / 51.694 ms |
| walking 在线CUDA语义＋G2-2S ORB-depth K=5 | 26.212 | 36.752 ms |
| sitting 在线CUDA语义＋G2-2S ORB-depth K=5 | 26.913 | 36.040 ms |
| fr1/xyz 在线CUDA语义＋G2-2S ORB-depth K=5 | 26.142 | 37.220 ms |

G0-4F shadow接近数据集帧率；补做的在线同步语义＋G2-1则明确未达到30 FPS。
因此当前结果不能证明：

```text
在线CUDA YOLO
+ 未来完整几何检测
+ 实际特征/地图过滤
```

已经稳定达到30 FPS。

## 12. GJ-1/GJ-2A结果与下一阶段

GJ-1已经独立实现Ji 2021的三维深度K-means区域生成。TUM fr3/walking与
fr1/xyz短序列中，每帧均得到24个非空cluster；可视结果符合“空间邻近表面分区，
一个对象可能跨多个cluster”的文献边界。

```text
fr3/walking GJ total mean: 57.84 ms
fr1/xyz GJ total mean: 58.45 ms
fr3/walking actual FPS: 12.56
```

因此GJ-1可以作为论文比较baseline，但当前全分辨率CPU K-means不能作为30 Hz主
方法。GJ-2也已完成：

```text
ORB feature -> raw-depth cluster
MapPoint + Tcw -> undistorted reprojection error
cluster support/error shadow statistics
```

GJ-2统计自身仅约0.023–0.028 ms。GJ-2A已经使用100帧walking离线person
proxy完成自动审计：

```text
全局Spearman(mean squared error, proxy fraction) = 0.308
Top-3 proxy capture = 36.05%
Top-3 area fraction = 19.10%
Top-3 random enrichment = 2.14×
逐帧Top-3 enrichment > 1: 85/99
```

optimizer outlier fraction的Top-3 capture仅为20.50%，说明cluster error不是简单
复述已有离群比例。但walking与fr1/xyz静态误差仍重叠，静态cluster mean error
最大值也达到约50 px。

因此GJ-2A通过的是“存在排序信号”门控，不是“动态阈值可靠”门控。继续执行边界：

1. 仍保持shadow-only；
2. 没有地图匹配支持的cluster标为unknown，不能标为static；
3. 不凭空补写论文未公开的`rho`、最小支持数或动态阈值；
4. 不与当前失败的flood fill混成一个算法；
5. 不修改Optimizer、g2o或后端；
6. 任何未来GJ实际过滤都需重新审批，当前GJ-3A适配已判定失败；
7. Ji baseline仍不满足30 Hz，主方法不采用全分辨率CPU K-means。

GJ-3P进一步核对了本地PaperNotes、论文PDF、官方arXiv源码包、IEEE元数据和
公开代码检索。确认论文只说明“选择较低阈值，阈值取决于匹配特征的平均重投影
误差”，没有给出`rho`形式、阈值函数/数值或最小支持数。因此严格二值判定无法
从公开材料补齐。未来若继续，只能建立单独标记为`GJ-A`的相对阈值适配shadow。

GJ-3A随后固定了`mean_squared_error_px2`、两种frame mean normalization、对数
lambda网格和calibration/validation拆分。两种normalization都在calibration选择
`lambda=0.5`，dynamic validation F1约`0.51`，但fr1/xyz静态选择面积约
`50%–54%`；同相机的fr3/sitting低动态诊断也约`50%–52%`。提高lambda会同时
快速压低动态召回，没有观察到明显可用工作点。因此GJ-3A冻结为负实验，不进入
实际动态cluster过滤。

## 13. G2-1多参考证据结果

G2-1根据DynaSLAM公开的“20帧参考数据库、最多5个参考帧”设置，建立了明确标记
为改造而非复现的shadow：

```text
最近5个成功关键帧
→ 复用G0稠密depth warp
→ 每像素comparison/positive/negative/consistent计数
→ 只保存二维直方图，不做动态决定
```

walking的离线person proxy审计显示，多参考重复证据比G0单参考直接seed更集中：

```text
C>=1, P>=1: precision 34.41%, conditional recall 23.77%
C>=2, P>=2: precision 38.54%, conditional recall 16.82%
C>=5, P>=2: precision 40.46%, conditional recall 17.60%
```

对应的fr1/xyz静态选择率分别约为`7.06%`、`2.65%`和`2.41%`。继续提高一致
票数虽然能压低静态选择率，但会使proxy recall快速降至约`10%`以下，因此尚未
观察到足以批准实际过滤的工作点。

实时性方面，5次全分辨率稠密warp本身平均约`20–23 ms`，三个短序列的端到端
实际速度约`20.96–22.17 FPS`，明确不满足30 FPS目标。该实现保留为shadow
对照，不进入同步主路径。

此外，sitting中曾长时间没有新关键帧，导致“最近关键帧”与当前帧相隔较远。
下一步必须把参考重叠、时间跨度和有效覆盖作为独立研究问题，不能直接把关键帧
票数解释为时序确认。

RTX 4060 Ti恢复后又完成三个199帧在线同步语义闭环。三次均为
`mask ready=199/199`、`mask age median/max=0/0`。在线G2-1耗时为：

```text
walking: 14.02 ms mean, 20.70 FPS end-to-end
sitting: 14.11 ms mean, 20.78 FPS end-to-end
fr1/xyz: 18.80 ms mean, 19.02 FPS end-to-end
```

语义动态深度从参考关键帧清除后，人物区域的几何参考覆盖显著下降。例如walking
在`C>=5`时，仅`1.94%`的person proxy像素具备5次比较；即便其条件召回看起来
较高，也不能把其余约`98%`无覆盖像素当作静态。该结果否定了“简单提高参考票数
即可完成语义＋几何融合”的解释，进一步确认未来G2-2必须独立建模：

```text
参考是否覆盖
残差是否支持动态
残差是否支持静态一致
```

不批准根据本轮在线结果选择任何`C/P`阈值。

## 14. G2-2R共视参考选择结果

G2-2R没有复制DynaSLAM官方代码中存在解释风险的位姿差排序，而是使用
ORB-SLAM2当前参考关键帧及其按共享MapPoint权重排序的共视邻居，再与20项几何
深度缓存求交。固定K选不满时只记录`computed=0`，不回退最近帧，也不把缺失
参考解释为静态票。

三个199帧在线CUDA同步语义实验均为`mask ready=199/199`、`mask age=0`。
结果显示：

```text
K=1 geometry mean: 3.34–4.34 ms，24.78–25.88 FPS
K=2 geometry mean: 6.08–7.96 ms，22.88–24.27 FPS
K=5 geometry mean: 13.91–18.66 ms，19.06–21.14 FPS
```

因此减少参考数能够近似线性降低稠密warp成本，但当前K=1仍未达到30 FPS。
K=5选满率为`92.4%–93.9%`；sitting中已选参考age最大达到198帧，
fr1/xyz达到170帧，证明高共视不等于时间新鲜。

在三个K共同成功计算的帧上，walking的K=5多数票`C>=3,P>=3`得到
`23.16%` person proxy precision和`55.53%` conditional recall，但fr1静态
背景率仍为`2.71%`，且person proxy不是真实运动GT。当前仍不批准任何C/P阈值
或SLAM过滤。

冻结结论：

```text
G2-2R = 已完成的参考选择shadow消融
G2-2S = 待复核后再决定的轻量空间采样
G1    = 仍不进入
```

## 15. G2-2S轻量采样结果

G2-2S采用DynaSLAM公开源码中可核对的feature-associated depth observation
思路：缓存关键帧`Frame::mvKeys`对应的有效静态深度，只投影这些稀疏样本。
共视参考、K=5、位姿、z-buffer、残差和证据分类保持不变。

在线CUDA成对实验显示：

```text
dense G2:     14.49 / 14.76 / 19.37 ms
orb-depth G2:  0.93 /  0.82 /  1.09 ms
orb-depth FPS: 26.21 / 26.91 / 26.14
```

上述成对对照的 dense 和 orb-depth 两边均包含旧 G0 单参考帧 dense shadow。
随后增加默认开启、仅由 G2-2S 专用配置关闭的
`Geometry.SingleReferenceShadowEnable`，得到隔离后的在线同步结果：

```text
walking: G2 0.779 ms mean, active 32.464 ms, 28.204 FPS
sitting: G2 0.866 ms mean, active 32.005 ms, 28.721 FPS
fr1/xyz: G2 1.005 ms mean, active 32.451 ms, 28.443 FPS
```

三次均为`mask ready=199/199`、`mask age=0`。G2-2S自身约1 ms的计算门控
已经通过，但端到端仍有`63–79/199`帧deadline overrun，实际速度未稳定达到
30 FPS。因此不能把此前26 FPS全部归因于稀疏几何，也不能根据active均值低于
33.33 ms就宣称30 FPS达成。

但walking、sitting、fr1/xyz的五参考任意比较覆盖分别从约
`48.66%/49.93%/71.85%`降到`0.47%/0.57%/0.91%`。同帧同参考审计确认，
sparse positive中约`84.81%–92.68%`也属于dense positive，但只召回dense
positive的约`53.74%–64.08%`。walking person proxy的无条件捕获从
`15.17%`降到`0.108%`。

因此：

```text
G2-2S = 计算成功、覆盖失败的稀疏证据baseline
G1-D  = 不进入
G1-F  = 仍需特征对应关系shadow，未解锁
G2成本 = 通过（约0.78–1.00 ms）
30FPS = 仍未稳定通过（约28.20–28.72 FPS）
```

## 16. 相关报告

- `results/g0_2a_2026-07-27/REPORT.md`
- `results/g0_2a_dynamic_2026-07-28/REPORT.md`
- `results/g0_4f_2026-07-28/REPORT.md`
- `results/gj_2026-07-28/GJ_IMPLEMENTATION_SPEC.md`
- `results/gj_2026-07-28/GJ1_DEPTH_CLUSTERING_RESULT.md`
- `results/gj_2026-07-28/GJ2_IMPLEMENTATION_SPEC.md`
- `results/gj2_2026-07-28/GJ2_CLUSTER_REPROJECTION_RESULT.md`
- `results/gj2a_2026-07-28/GJ2A_PROXY_AUDIT_SPEC.md`
- `results/gj2a_2026-07-28/GJ2A_CLUSTER_PROXY_AUDIT_RESULT.md`
- `results/gj3_literature_audit_2026-07-28/GJ3_PARAMETER_PROVENANCE_AUDIT.md`
- `results/gj3a_2026-07-28/GJ3A_THRESHOLD_SHADOW_SPEC.md`
- `results/gj3a_2026-07-28/GJ3A_THRESHOLD_SHADOW_RESULT.md`
- `results/g2_1_2026-07-28/G2_1_MULTI_REFERENCE_SHADOW_SPEC.md`
- `results/g2_1_2026-07-28/G2_1_MULTI_REFERENCE_SHADOW_RESULT.md`
- `results/g2_2r_2026-07-28/G2_2R_COVISIBILITY_REFERENCE_SHADOW_SPEC.md`
- `results/g2_2r_2026-07-28/G2_2R_COVISIBILITY_REFERENCE_SHADOW_RESULT.md`
- `results/g2_2s_2026-07-28/G2_2S_ORB_DEPTH_SAMPLING_SHADOW_SPEC.md`
- `results/g2_2s_2026-07-28/G2_2S_ORB_DEPTH_SAMPLING_SHADOW_RESULT.md`
- `results/g2_2g_2026-07-28/G2_2G_GRID_DEPTH_SAMPLING_SHADOW_SPEC.md`
- `results/g2_2g_2026-07-28/G2_2G_GRID_DEPTH_SAMPLING_SHADOW_RESULT.md`
- `results/g2_3r0_2026-07-28/G2_3R0_DEPTH_BOUNDARY_PARTITION_SHADOW_SPEC.md`
- `results/g2_3r0_2026-07-28/G2_3R0_DEPTH_BOUNDARY_PARTITION_SHADOW_RESULT.md`
- `results/g2_3r1_2026-07-28/G2_3R1_REGION_EVIDENCE_AGGREGATION_SHADOW_SPEC.md`
- `results/g2_3r1_2026-07-28/G2_3R1_REGION_EVIDENCE_AGGREGATION_SHADOW_RESULT.md`
- `results/g2_3r2_2026-07-29/G2_3R2_DENSE_GRID_REGION_COVERAGE_AUDIT_SPEC.md`
- `results/g2_3r2_2026-07-29/G2_3R2_DENSE_GRID_REGION_COVERAGE_AUDIT_RESULT.md`
- `results/g2_3r3_2026-07-29/G2_3R3_PYRAMID_DENSE_EVIDENCE_SHADOW_SPEC.md`
- `results/g2_3r3_2026-07-29/G2_3R3_PYRAMID_DENSE_EVIDENCE_SHADOW_RESULT.md`
- `results/ate_semantic_baseline_2026-07-29/REPORT.md`
- `results/experiments.md`

## 17. G2-2G规则深度网格采样结果

G2-2S的ORB-depth采样只有约`0.47%–0.91%`像素覆盖，不能输出动态深度区域。
在进入复杂区域分割前，G2-2G用标准规则栅格抽样测量密度、覆盖和耗时曲线。它
是工程控制，不是DynaSLAM、DetectFusion或SInDSLAM复现。

walking前199帧在线CUDA同步语义结果：

```text
stride 2: coverage 33.300%, G2 3.594 ms, 26.533 FPS
stride 4: coverage 11.674%, G2 1.598 ms, 28.118 FPS
stride 8: coverage  3.135%, G2 1.337 ms, 28.165 FPS
```

stride 4跨序列的G2成本为`1.598–1.993 ms`，覆盖为
`11.674%–16.388%`，实际速度为`28.077–28.318 FPS`。同帧同位姿同参考审计
中，grid positive有`97.24%`也受dense positive支持，但walking person proxy
无条件捕获只有`2.11%`，dense为`16.61%`。

因此：

```text
G2-2G = 已完成的中间密度负对照
grid s4 = 保留为未来区域模块的采样/速度baseline
G1-F = 不进入
G1-D = 不进入
```

下一阶段不再继续调stride或做无边界膨胀，而应依据DetectFusion/SInDSLAM先冻结
有边界的轻量区域表示Shadow，并把区域生成与区域证据判定拆开。

## 18. G2-3R0深度边界区域划分Shadow

G2-3R0依据本地SInDSLAM原文的深度不连续边界：

```text
δ_depth > max(τ1 * depth, τ2)
τ1=0.025，τ2=0.08m
```

生成boundary mask和四邻域connected-component标签。它没有实现原文的K-means、
平面边缘、重聚类或光流，只是`[A]`轻量区域表示适配；也没有读取residual或判断
动态。

三个199帧深度审计结果：

```text
walking: largest/valid mean 47.58%, top5 86.46%, 3.00 ms
sitting: largest/valid mean 45.10%, top5 89.74%, 3.16 ms
fr1/xyz: largest/valid mean 75.16%, top5 95.53%, 3.38 ms
```

代表帧中人物和若干独立物体通常能被深度边界切开，区域拓扑比G0-3的无界
all-seed flood fill更合理。但TUM3约86%的区域不超过64像素，前五大区域又覆盖
大部分有效深度；因此既有碎片化，也仍有大背景区域。任何“区域中有一个seed就
整区动态”的规则继续禁止。

全分辨率CPU成本约3ms，当前同步实时门未通过。冻结结论：

```text
G2-3R0 topology = 初步通过
G2-3R0 runtime  = 未通过
dynamic decision = 未实施
G1-F / G1-D = 继续锁定
```

下一步只允许在Shadow中冻结G2-3R1：对固定区域统计comparison、positive和
consistent证据直方图，不选动态阈值、不接Tracking过滤，并单独核算区域生成与
证据聚合成本。

## 19. G2-3R1区域内多参考证据聚合Shadow

G2-3R1依据DetectFusion的“motion evidence与geometry segment重叠”和
SInDSLAM的“cluster内残差类别与动态像素比例”结构，将G2-2G证据聚合到
G2-3R0固定区域。它不复制ICP residual、光流、双阈值或动态判决。

三个199帧在线CUDA同步语义实验显示：

```text
walking person-proxy:
  comparison coverage 3.75%
  positive / compared 63.42%

sitting person-proxy:
  comparison coverage 2.79%
  positive / compared 13.03%

fr1/xyz nonsemantic:
  comparison coverage 21.85%
  positive / compared 4.77%
```

walking已比较的person像素存在较强运动信号，但96.25%的person proxy像素没有
当前几何比较，不能解释为静态。区域级`positive_vote_ratio`对walking
semantic-majority与fr1静态背景的诊断AUC约`0.647–0.775`，并随最小区域和
支持量明显变化；不批准任何动态阈值。

运行成本：

```text
G2 evidence:      1.60–1.91 ms
region partition: 3.00–3.33 ms
region aggregate: 0.53–0.64 ms
actual FPS:       25.90–26.55
```

冻结结论：

```text
G2-3R1 statistics = 通过
dynamic decision  = 未通过、未实施
30 FPS             = 未通过
G1-F / G1-D        = 继续锁定
```

下一步不调positive阈值。先分别检查低分辨率区域表示的成本/边界保真，以及同帧
dense evidence相对于stride-4的区域覆盖上限。

## 20. 2026-07-29正式ATE/RPE semantic baseline

在完整TUM `fr3_walking_xyz` 827帧一对一关联上，分别运行原始RGB-D baseline
和修复后的semantic-only各三次。两组均关闭Geometry。

ATE采用无尺度修正的SE(3) Umeyama alignment，RPE采用相邻一帧translation
error。三次结果：

```text
baseline ATE RMSE:
0.940707 / 0.730574 / 0.690009 m

semantic-only ATE RMSE:
0.016477 / 0.017752 / 0.014428 m

baseline RPE RMSE:
0.025407 / 0.024644 / 0.042619 m/frame

semantic-only RPE RMSE:
0.011858 / 0.012771 / 0.011876 m/frame
```

中位数ATE由`0.730574m`降至`0.016477m`，下降`97.74%`；中位数RPE下降
`53.26%`。baseline第三次只写出814/827 poses，semantic-only三次均为
827/827 poses，并且mask均为827/827 ready、age=0。

冻结结论：

```text
walking semantic trajectory baseline = 通过并冻结
person proxy = 仅保留为几何局部证据诊断
geometry ATE = 等G1-F真正改变观测后再测
```

下一步先扩展到低动态`fr3_sitting_static`，验证semantic-only不会因静止人物
语义先验造成明显轨迹退化。

## 21. 2026-07-29 sitting_static ATE/RPE/FPS扩展

在TUM `fr3_sitting_static`的680帧严格一对一关联上，baseline和
semantic-only各独立运行三次。两组均关闭Geometry；语义组使用CUDA并且三次
都是680/680 mask ready、mask age median/max为0/0。

三次中位数：

```text
                           baseline       semantic-only
ATE RMSE                  0.008022 m     0.006482 m
translation RPE RMSE      0.005335 m     0.005492 m
rotation RPE RMSE         0.163444 deg   0.165418 deg
actual FPS                28.5124        28.5243
完整轨迹                  3/3            3/3
```

客观解释：

```text
ATE改善19.20%
translation RPE恶化2.94%
rotation RPE恶化1.21%
FPS中位数差0.04%，应视为相同播放速度
无轨迹缺帧
```

因此`no-major-regression`门通过，但不能声称低动态序列的所有指标都改善。
语义组deadline miss为`29/14/32`，baseline为0；同步语义仍占用约12.4ms
median，30FPS计算预算未严格通过。

六次ATE均使用独立时间关联和无尺度Umeyama SVD交叉校验，与evo一致。正式结果
见：

- `results/ate_semantic_baseline_2026-07-29/REPORT.md`

当前冻结：

```text
Phase 0 semantic baseline = walking + sitting_static正式轨迹对照完成
geometry shadow metrics   = 继续报告局部证据、覆盖和成本
geometry ATE              = 等G1-F真实过滤观测后再测
```

## 22. G2-3R2 dense-vs-grid区域覆盖上限

G2-3R2在完全相同的当前帧、5个共视参考、初始位姿和深度区域标签下，成对比较
stride-4与full-resolution dense多参考证据。它是覆盖/成本实验控制，不是新的
动态检测算法。

三个约199帧在线CUDA语义实验：

```text
             grid coverage   dense coverage   grid/dense保留
walking          18.27%          77.70%           23.51%
sitting          18.17%          73.25%           24.81%
fr1/xyz          21.90%          94.90%           23.08%
```

walking person proxy comparison coverage从`3.74%`提高到`27.17%`，且已比较
像素中的positive比例为`68.05%`。但walking非semantic背景positive也从
`9.37%`提高到`17.26%`，所以更多证据不等于更高动态精度。

成本：

```text
grid G2:   1.55–1.89 ms
dense G2: 14.00–18.88 ms
partition: 2.91–3.24 ms
审计FPS:  17.93–20.27
```

冻结结论：

```text
stride-4 coverage bottleneck = 已确认
dense coverage upper bound   = 通过
dense realtime gate          = 未通过
dynamic decision             = 未通过
G1-F / G1-D                  = 继续锁定
```

这仍符合原计划的G2-Robust“多参考＋区域评分”范围。参考选择、采样、区域划分和
覆盖审计只是把大阶段拆成可验证的小实验。原计划中的MAD和时序被有意暂缓，因为
当前首先需要解决覆盖与静态假positive，不属于研究方向偏移。

下一步不再调stride，也不直接上线dense。先从本地SInDSLAM/PaperNotes核对
Gaussian pyramid/coarse-to-fine依据，冻结低分辨率dense evidence shadow的
内参缩放、深度下采样、回投区域和覆盖/耗时验收标准。

## 23. G2-3R3二分辨率dense evidence Shadow

文献复核确认：

```text
KinectFusion:
  boundary-preserving depth pyramid + multi-scale dense ICP

SInDSLAM:
  depth Gaussian pyramid只用于K-means clustering

DetectFusion:
  coarse-to-fine pyramid只用于tracking
```

因此G2-3R3的低分辨率动态证据被明确标为`[S/H]`工程假设，不归因成
SInDSLAM复现。

实现scale-2 boundary-preserving depth pyramid、缩放内参、参考half-depth缓存、
320x240 dense multi-reference warp和nearest cell expansion。三序列相对同参考
full dense：

```text
             pyramid coverage   dense coverage   数量保留
walking           76.94%           77.40%         99.40%
sitting           73.09%           73.52%         99.42%
fr1/xyz           94.78%           95.41%         99.34%
```

同像素positive presence agreement为`94.80%–98.10%`，但exact vote agreement
只有`52.22%–76.98%`。因此只批准区域级近似，不批准逐像素vote判决。

成本：

```text
pyramid evidence: 4.92–6.07 ms
full dense audit: 14.25–18.96 ms
region partition: 2.93–3.26 ms
```

关闭full dense audit后的候选-only系统：

```text
walking: 24.99 FPS
sitting: 24.96 FPS
fr1/xyz: 24.30 FPS
```

冻结结论：

```text
pyramid coverage gate = 通过
pixel exact-vote gate = 未通过
30 FPS gate           = 未通过
dynamic decision      = 未实施
G1-F / G1-D           = 继续锁定
```

下一步只允许G2-3R4：复用同一个scale-2 depth pyramid构造低分辨率region
representation，测量相对全分辨率区域的边界/区域保真和约3ms partition成本能否
降低。即使完全省去该3ms，active total也可能仍高于33.3ms。G2-3R4后应先验证
动态/静态区分能力；只有检测有效后，才根据实测决定是否值得继续CPU优化或研究
semantic/geometry调度。

## 24. G2-3R4实现前文献审计与规格冻结

G2-3R4已经完成实现前文献核对和Shadow规格草案，尚未修改代码或运行实验。

文献边界重新冻结为：

```text
[L/A] KinectFusion只支持boundary-preserving depth pyramid组件；
[L]   SInDSLAM的Gaussian pyramid用于K-means coarse-to-fine初始化；
[L]   DetectFusion的三层pyramid用于tracking；
[S/H] scale-2域的当前region partition及其full-domain映射是本项目假设。
```

当前full-resolution partition只是高分辨率参考实现，不是object-region GT。
低分辨率和高分辨率region ID均为任意编号，G2-3R4禁止直接比较ID，改为使用：

```text
contingency table
best-overlap IoU
pairwise co-membership
一对多/多对一mapping
相对reference的merge/fragmentation mass
boundary distance与容差precision/recall
ORB feature co-membership
```

native half-resolution evidence每个cell只能计一次，不得把2x2 expansion解释成
四次独立vote。静态相关统计只称risk proxy，不称dynamic FPR。

G2-3R4只是一轮有界优化实验。当前active mean减去全部full partition成本后仍约
为`35.664/35.884/36.733 ms`，所以本阶段不以30FPS为通过条件。规格预先设置
结构安全、evidence assignment和净节省门槛；若结构或收益失败即停止该路线。
即使通过，下一步也优先验证动态/静态区分能力，不默认继续CPU优化。

设计文档：

- `results/g2_3r4_2026-07-29/G2_3R4_LOW_RESOLUTION_REGION_LITERATURE_AUDIT.md`
- `results/g2_3r4_2026-07-29/G2_3R4_LOW_RESOLUTION_REGION_APPROXIMATION_SHADOW_SPEC.md`

当前状态：

```text
G2-3R4 literature audit = 完成
G2-3R4 SPEC             = 待评审
G2-3R4 implementation   = 未开始
G2-3R4 RESULT           = 不存在
G1-F / G1-D             = 继续锁定
```

## 25. G2-3R4最小实现与有界收益停止

G2-3R4已完成一个默认关闭的最小实现、确定性测试和三序列candidate-only
计时。native scale-2 region path mean为`1.231/1.320/1.315 ms`，P95为
`1.512/1.594/1.692 ms`；相对旧full region path的隔离估算节省为
`2.287/2.401/2.560 ms`。

但端到端active mean只改善`1.600/0.287/1.403 ms`。预冻结门要求三个序列
分别至少改善`1.5 ms`，因此overall失败。

按停止条件：

```text
G2-3R4 bounded benefit gate = 失败
full-vs-half structure audit = 未执行，所有结构指标保持未知
low-resolution candidate     = 默认关闭，不保留
dynamic decision             = 未实施
G1-F / G1-D                  = 继续锁定
```

下一步不继续CPU微调，返回动态/静态区分能力和风险代理验证。

## 26. G2-4动态/静态区分能力初步审计

新增`[S] G2-4`阶段名，用于geometry evidence dynamic/static separability
shadow gate，不对应任何论文的阶段编号。

只读复用G2-3R3三个region CSV后得到：

```text
walking semantic-vs-background proxy AUC = 0.557
sitting semantic-vs-background proxy AUC = 0.444
fr1/xyz proxy AUC                         = 0.545
```

这些都是proxy AUC，不是motion AUC。walking在161个eligible frame中的
within-frame weighted-score delta中位数为`0.166`，但P10为`-0.114`，只有
`69.6%` frame为正；sitting中位数为`-0.117`，说明person proxy不能作为通用
动态标签。

fr1/xyz static risk proxy显示，小区域/低comparison support会产生大量极端
region score。score至少`0.5`的region占`15.27%`，但只占`0.29%`
comparison-vote mass。这不是static FPR，而是强support confounding证据。

冻结状态：

```text
G2-4A preliminary audit       = 完成
general separability gate     = 未通过、不可判定
dynamic threshold             = 未选择
classification output         = none
G1-F / G1-D                   = 继续锁定
```

下一小步只允许补充boundary、invalid/occlusion、reference support和pose-risk
instrumentation，并制定Bonn static/moving-box的低人工负担评价协议。Bonn官方
提供camera-pose GT和static 3D model，但未声明提供逐帧dynamic mask。

文档：

- `results/g2_4_2026-07-29/G2_4_DYNAMIC_STATIC_SEPARABILITY_LITERATURE_AUDIT.md`
- `results/g2_4_2026-07-29/G2_4_DYNAMIC_STATIC_SEPARABILITY_SHADOW_SPEC.md`
- `results/g2_4_2026-07-29/G2_4_PRELIMINARY_SEPARABILITY_RISK_AUDIT_RESULT.md`

## 27. G2-4A风险字段与Bonn评价协议

完成一个默认关闭的纯诊断instrumentation小步骤：

```text
boundary distance 1/2 pixel bands
invalid-depth distance 1/2 pixel bands
single/multi-reference support
unanimous-positive support
```

每个band只记录region/comparison/positive pixel与vote mass，不生成motion
score。开关：

```text
Geometry.RegionRiskDiagnosticsEnable
DT_SLAM_GEOMETRY_REGION_RISK_DIAGNOSTICS
```

默认关闭，避免污染G2-3性能基线。构建、确定性测试和30帧smoke均通过，
CSV invariant通过，日志保持：

```text
dynamic_decision=none
direct_slam_state_mutation=none
```

最终pose-quality没有接入，因为当前geometry调用发生在`TrackLocalMap()`前，
此时`mnMatchesInliers`不是当前帧最终值，不能作为事实记录。

Bonn协议核对：

- 本地已有moving nonobstructing/obstructing box zip；
- 本地尚无Bonn static archive；
- 官方提供camera pose GT和static 3D model，不提供已声明的逐帧motion mask；
- Bonn具有非零distortion，运行前必须统一RGB/depth/ORB/mask/geometry坐标域；
- 先自动选帧和预标注，用户只修正少量困难边界和运动状态。

文档：

- `results/g2_4_2026-07-29/G2_4A_RISK_INSTRUMENTATION_RESULT.md`
- `results/g2_4_2026-07-29/G2_4_BONN_STATIC_MOVING_BOX_EVALUATION_PROTOCOL.md`

下一小步是Bonn坐标域只读审计与最小设计，不是动态阈值。

## 28. G2-4B Bonn坐标域审计与最小一致域规格

只读代码审计纠正了一个容易误述的点：

```text
Frame::ComputeStereoFromRGBD:
  depth在raw mvKeys位置读取；
  mvKeysUn用于去畸变后的针孔投影。
```

因此当前RGB-D feature depth路径本身不是“用undistorted keypoint读取raw
depth”。真正冲突是geometry warp直接把raw depth pixel当无畸变针孔域，而
Bonn官方相机具有非零distortion。

`[S]` 用官方`K,D`测得raw-to-undistorted像素偏移median `0.675 px`、
P95 `1.208 px`、max `4.227 px`，不能相对当前1/2-pixel risk band忽略。
该测量不证明动态精度会提高或下降。

冻结最小候选：

```text
RGB:   official K,D -> P=K linear rectification -> YOLO + ORB
depth: official K,D -> P=K nearest rectification -> tracking + geometry
mask:  YOLO直接在rectified RGB上产生
SLAM:  K=official K, distortion=0
```

这是`[A/S/H]`组合，不是论文整体复现。`P=K`网格审计显示99.401%输出像素
映射到图像内source，但这不等于有效depth retention或对象边界正确，仍需真实
样本门控。

本地archive审计还发现：

```text
moving_nonobstructing_box: depth.txt比实际depth PNG多4项
moving_obstructing_box:    depth.txt比实际depth PNG多3项
```

后续association必须检查一对一时间阈值和文件存在性。

文档：

- `results/g2_4_2026-07-29/G2_4B_BONN_COORDINATE_DOMAIN_AUDIT_AND_SPEC.md`

当前状态：

```text
G2-4B coordinate audit = 完成
G2-4B implementation   = 未开始
dynamic threshold      = 未选择
G1-F / G1-D            = 继续锁定
```

下一小步只允许实现默认关闭的Bonn输入联合rectification和确定性坐标域测试。

## 29. G2-4B联合rectification实现与部分通过

新增默认关闭的`RGBDInputRectifier`：

```text
RGB   -> INTER_LINEAR
depth -> INTER_NEAREST
P     -> official K
Camera distortion after rectification -> zero
```

配置拒绝double-undistort和tracking/geometry K不一致；precomputed mask还必须
显式声明`undistorted_pinhole`域。未修改YOLO实现、Optimizer或SLAM过滤状态。

确定性测试、构建和TUM默认关闭3帧smoke通过。Bonn前30帧真实数据：

```text
valid-depth retention mean     = 99.5246%
nearest-depth value violations = 0
alignment risk proxy raw/rect  = 0.747225 / 0.747480
```

alignment只是边缘风险代理，不是真值。

150帧geometry shadow：

```text
input rectification mean/P95 = 0.631/0.715 ms
region rows                  = 2107 across 111 frames
active total mean            = 26.894 ms
deadline misses              = 2/150
actual FPS                   = 29.718
CSV risk invariants          = 0 violations
dynamic decision             = none
direct SLAM mutation         = none
```

短smoke timing不作为正式性能结论。

online YOLO在沙箱内曾因`/dev/nvidia*`不可见而失败；`nvidia-smi`在host可见
RTX 4060 Ti，因此不能写成主机无GPU。失败日志保留。

在host GPU完成30帧online smoke：

```text
mask ready/age median/max       = 30/30, 0/0 frame
input rectification mean/P95   = 0.637/0.762 ms
semantic block steady mean     = 8.904 ms
active total mean              = 26.574 ms
deadline misses                = 0/30
actual FPS                     = 29.724
```

YOLO接收rectified RGB；ORB、mask、registered depth和geometry的统一坐标域
已经实测通过。短smoke不是正式性能报告。

文档：

- `results/g2_4_2026-07-29/G2_4B_BONN_COORDINATE_DOMAIN_RESULT.md`

冻结状态：

```text
RGB/depth/ORB/geometry coordinate subgate = 通过
online semantic mask coordinate subgate   = 通过（host GPU）
dynamic/static separability               = 未验证
G1-F / G1-D                               = 继续锁定
```

下一小步是不修改YOLO代码地自动选帧并审计box semantic coverage，先区分
semantic-covered和semantic-uncovered box evidence，不选择动态阈值。

## 30. G2-4C Bonn自动候选选帧

新增GT camera-motion-compensated scale-4 RGB-D选帧工具。所有输出层均使用
`proxy_*`名称，不解释为box motion GT。

第一版虽然各层6帧且无重复，但hold-out low-inconsistency层包含连续近重复
box close-up，high/transition也出现相邻帧，因此冻结为diversity失败并保留。
没有调整geometry proxy；第二版只增加固定temporal/thumbnail diversity。

第二版：

```text
                         nonobstructing   obstructing
association frames          778             589
eligible frames             772             587
selected frames              24              24
unique frames                24              24
global min frame gap          5               6
```

每条序列四个proxy层各6帧。同层minimum frame gap为`20–41`，minimum
thumbnail MAD为`0.052–0.185`。synthetic identity/translation self-test通过。

文档：

- `results/g2_4_2026-07-29/G2_4C_BONN_AUTOMATIC_FRAME_SELECTION_SPEC.md`
- `results/g2_4c_2026-07-29/G2_4C_BONN_AUTOMATIC_FRAME_SELECTION_RESULT.md`

冻结状态：

```text
automatic review candidates = 通过
box motion labels           = 不存在
box semantic coverage       = 未审计
dynamic threshold           = 未选择
G1-F / G1-D                 = 继续锁定
```

下一小步是不修改YOLO地导出48个候选帧的exact C++ person mask/detection，并
生成box预标注与coverage review页面。当前C++ semantic只筛选COCO class 0
person，所以box不会被类别直接识别，但人与box接触时的间接mask覆盖仍需实测。

## 31. G2-4C 独立审阅修正：时间戳与 Hold-out 身份

独立审阅发现原 G2-4C 有两项需要修正：

1. `moving_obstructing_box` 已被 geometry proxy 排序、生成 contact sheet，
   并参与第一版 diversity 失败后的规则修正，因此不能再称 strict hold-out；
2. 原选帧工具用 RGB timestamp 插值 GT pose，但 depth warp 的物理观测时刻
   对应 depth timestamp。

修正：

- 两条当前 moving-box 序列统一为 development/review；
- strict unknown-dynamic hold-out 尚未选择；
- 工具新增 `--pose-timestamp-source rgb|depth`，默认 `depth`；
- JSON 明确记录：

```text
selection_conditioned_on_geometry_proxy=true
selection_is_holdout_evaluation=false
suitable_for_unbiased_sequence_metrics=false
```

从本地 zip 重新解压、过滤不存在深度文件并重建 association，四组重跑全部
成功：

```text
nonobstructing RGB-time / depth-time
obstructing    RGB-time / depth-time
```

| Sequence | Inconsistency Pearson | Median absolute delta | P95 absolute delta | 24-frame overlap |
| --- | ---: | ---: | ---: | ---: |
| nonobstructing | 0.8929 | 0.00205 | 0.02416 | 14/24 |
| obstructing | 0.9267 | 0.00149 | 0.02446 | 12/24 |

整体 proxy 相关较高，但 top-ranked 候选对时间戳明显敏感；obstructing 的
`proxy_transition` 同角色重叠为 `0/6`。因此后续开发期 review 使用 depth-time
候选，RGB-time 只作为 pipeline-matched sensitivity 对照。

冻结边界：

```text
selected 24-frame subset != unbiased sequence evaluation
person mask               != box mask
box pre-annotation        must be independent of evaluated geometry evidence
dynamic threshold         = none
G1-F / G1-D              = locked
```

详细结果：

```text
results/g2_4c_correction_2026-07-29/
  G2_4C_TIMESTAMP_AND_HOLDOUT_CORRECTION_RESULT.md
```

修正后的下一小步：

1. 导出 48 个 depth-time development candidates 的实际 C++ person detection；
2. 导出实际进入语义过滤的、实例合并且膨胀后的 person union mask；
3. 使用独立于 positive residual/region score 的方法生成 box 预标注；
4. 只做 semantic coverage review，不选择动态阈值；
5. 在参数和标签协议冻结前，选择并封存一个尚未运行 geometry proxy 的 strict
   hold-out。

## 32. G2-4D 真实 C++ Person 导出与独立 Box 粗框审计

已对修正后的 48 个 depth-time development candidates 完成实际 C++ 语义
导出：

```text
moving_nonobstructing_box: 24/24
moving_obstructing_box:    24/24
requested seq == mask seq: 48/48
rectified size:            640x480
```

新增：

- `DT-SLAM/Examples/RGB-D/semantic_review_export.cc`
- `DT-SLAM/tools/audit_bonn_semantic_box_coverage.py`
- `results/g2_4d_2026-07-29/G2_4D_PERSON_EXPORT_AND_BOX_PREANNOTATION_SPEC.md`
- `results/g2_4d_2026-07-29/G2_4D_PERSON_EXPORT_AND_BOX_PREANNOTATION_RESULT.md`

独立导出程序只复用当前 `RGBDInputRectifier` 和 `YOLOSegment`，没有修改 YOLO
后处理或 Tracking。

首次严格 0/255 验收暴露了真实 mask 约定：YOLO实例 mask resize 使用线性
插值，最终 `CV_8U` mask 的边缘包含 `1..254`。Tracking 会执行
`compare(mask,0,CMP_NE)`，因此真实过滤约定是：

```text
0       = not filtered
nonzero = person-filtered
```

两序列的中间值像素分别占 nonzero mask 的 `13.53%` 和 `10.88%`。本阶段只
记录该风险，没有修改冻结的 semantic baseline。

actual C++ person 结果：

| Sequence | person detections | zero-mask frames |
| --- | ---: | ---: |
| nonobstructing | 5 | 19/24 |
| obstructing | 4 | 20/24 |

随后仅查看 rectified RGB 和 source frame id，制作
`agent_rgb_only_coarse_bbox_v1` target-box 预标注；没有读取 residual、
region score、proxy role/rank/value 或 geometry mask。预标注明确为
`unverified`、`is_ground_truth=false`。

覆盖审计：

| Sequence | target visible/partial | visible frames with person mask | bbox/person coverage median |
| --- | ---: | ---: | ---: |
| nonobstructing | 24 | 5 | 0.00% |
| obstructing | 16 | 2 | 0.00% |

所有非零重叠均发生在人物遮挡或搬运箱子的帧。无人物的箱子帧中，当前
person-only 语义 mask 为零。这说明语义分支存在预期的未知类别缺口，但粗框
不是像素 GT，不能据此报告 box segmentation recall 或动态识别率。

冻结状态：

```text
G2-4D exact semantic export       = 通过
G2-4D development coverage review = 完成
box pixel/motion ground truth      = 不存在
dynamic threshold                 = none
direct SLAM mutation              = none
G1-F / G1-D                       = locked
```

下一步不直接选阈值。应先把 target visibility/粗框作为独立开发标签，与完整
连续序列产生的 C++ geometry risk/evidence 字段做成对审计，并在任何规则冻结
前选择一个尚未运行 geometry proxy 的 strict hold-out。

## 33. G2-4E 连续 Development Evidence 审计

G2-4E 已完成，仍为 shadow-only。

首先封存了尚未运行 geometry proxy、未生成联系表的：

```text
BONN/rgbd_bonn_balloon_tracking.zip
size   = 325919363 bytes
sha256 = 3c63ec5d06ffc7b97f2f3f965f4bdf7e52b72f38cd98e0b532456e0ef7e3c421
```

该 archive 未解压、未查看、未运行。只有在 evidence 定义、判决规则和
motion-label protocol 冻结后才能解封。

两条 development 序列完成全长 online CUDA semantic + geometry shadow：

| 指标 | nonobstructing | obstructing |
| --- | ---: | ---: |
| 输入帧 | 778 | 589 |
| person mask ready | 778/778 | 589/589 |
| mask age median/max | 0/0 | 0/0 |
| region CSV 行 | 9,149 | 8,074 |
| active total mean | 41.107 ms | 38.156 ms |
| actual FPS | 24.109 | 25.750 |

新增 exact partition 审计工具：

- `DT-SLAM/Examples/RGB-D/box_region_partition_audit.cc`
- `DT-SLAM/tools/audit_bonn_box_region_evidence.py`

它们复用相同的 rectification、米制深度转换和
`PartitionDepthByDiscontinuity()`。48 个候选中有在线 geometry evidence 的
42 帧，离线重建与在线 partition 为 `42/42` exact match；其余 6 帧属于序列
早期无可用多参考证据，不能解释为静态。

关键负门控结果：

- nonobstructing 的 18 个 `target visible + person absent` 帧中，bbox 主导
  region 的 positive/comparison 中位数为 `2.227%`，整帧为 `2.224%`；
- 该主导 region 只有 `15.02%` 位于 bbox 内，说明约 85% 延伸到背景；
- obstructing 的 9 个无人物可见箱子帧中，主导 region positive/comparison
  为 `2.590%`，整帧为 `2.623%`；
- obstructing 的 8 个 target-absent 帧整帧 positive/comparison 反而为
  `4.148%`；
- 明显高响应主要出现在 person-present 帧，而这些人物已由 semantic branch
  覆盖。

因此当前固定 depth-region 聚合不能可靠隔离未知箱子，也没有可批准的
dynamic threshold：

```text
dynamic_decision            = none
direct_slam_state_mutation   = none
G1-F / G1-D                  = locked
strict hold-out              = sealed and unopened
```

该结果不否定 direct per-pixel depth-warp evidence；它否定的是“当前固定
depth region 聚合已经足以形成动态判决”。粗 bbox 和 visibility 也不是
pixel/motion GT，不能报告未知箱子的 precision/recall。

下一步先核对本地 PaperNotes/PDF，再选择最小、可追溯的证据路线。不得直接
拟合组合阈值，也不得解封 hold-out。

详细文档：

- `results/g2_4e_2026-07-29/G2_4E_CONTINUOUS_DEVELOPMENT_EVIDENCE_AUDIT_SPEC.md`
- `results/g2_4e_2026-07-29/G2_4E_CONTINUOUS_DEVELOPMENT_EVIDENCE_AUDIT_RESULT.md`
- `results/g2_4e_2026-07-29/STRICT_HOLDOUT_MANIFEST.md`

## 34. G2-4F0 Direct Multi-reference ORB Feature Evidence

G2-4F0 已完成，仍为 shadow-only。

本阶段先核对 Ji 2021、SInDSLAM、DetectFusion、NGD-SLAM、DVI-SLAM 的本地
PaperNotes/原始 PDF，并将 FlowFusion 的自运动补偿 flow 只保留为下一项
failure-driven 候选。当前没有复现这些完整系统。

新增：

- `GeometricDynamicDetector::SampleMultiReferenceEvidenceAtFeatures()`；
- 可选逐 ORB feature 的 multi-reference vote CSV；
- native pyramid cell 映射与去重复统计；
- bbox/person 分层离线审计；
- 确定性测试和脚本 self-test。

两条 Bonn development 序列完成 online CUDA 运行：

| 指标 | nonobstructing | obstructing |
| --- | ---: | ---: |
| 帧数 | 778 | 589 |
| mask ready / age | 778/778，0/0 | 589/589，0/0 |
| feature rows | 23,097 | 17,794 |
| actual FPS | 24.575 | 25.931 |

48 个候选中有 42 帧具备在线多参考 evidence；缺失的 6 帧与 G2-4E 一致，均
保持 `no evidence`。

关键结果：

| 无人物箱子条件 | nonobstructing | obstructing |
| --- | ---: | ---: |
| 可用帧 | 18 | 9 |
| 框内 comparison coverage 中位数 | 95.801% | 93.413% |
| 框内 positive-presence 中位数 | 1.746% | 4.060% |
| 框外 positive-presence 中位数 | 6.703% | 6.704%（6帧） |
| presence enrichment 中位数 | 0.289× | 0.472×（6帧） |
| 框内 positive vote ratio 中位数 | 0.545% | 1.224% |
| vote enrichment 中位数 | 0.205× | 0.428×（6帧） |
| unanimous-positive 中位数 | 0 | 0 |

comparison 与 multi-reference coverage 充足，但箱框内正证据没有相对背景
富集。nonobstructing 的 person-present 5 帧则出现 `1.966×/2.333×` 的
presence/vote enrichment，说明强响应主要与人物搬运/遮挡混杂。obstructing
的 target-absent 8 帧全局 positive-presence/vote 中位数
`9.198%/5.042%`，也高于无人物 target-visible 框内值。

所以当前负门控进一步定位为：

```text
失败不只来自 depth-region aggregation；
当前 direct positive depth evidence 在这些无人物箱子候选上也没有局部富集。
```

但候选帧没有独立 motion label，粗框也不是 pixel GT，不能把该结果扩张为
“所有 depth-warp 几何无效”，也不能报告 precision/recall。

冻结状态：

```text
dynamic_decision          = none
direct_slam_state_mutation = none
strict hold-out           = sealed and unopened
G1-F / G1-D               = locked
```

下一步进入 G2-4F1 文献审计与 SPEC：只研究稀疏
ego-motion-compensated optical-flow residual 是否能提供与当前 depth residual
互补的 shadow evidence，同时建立独立 motion-state 自动预标注，不能直接融合、
调阈值或过滤。

详细文档：

- `results/g2_4f_2026-07-29/G2_4F_DIRECT_FEATURE_EVIDENCE_LITERATURE_AUDIT.md`
- `results/g2_4f_2026-07-29/G2_4F0_DIRECT_MULTIREFERENCE_FEATURE_EVIDENCE_SPEC.md`
- `results/g2_4f_2026-07-29/G2_4F0_DIRECT_MULTIREFERENCE_FEATURE_EVIDENCE_RESULT.md`

## 35. G2-4F1 Sparse Ego-flow：文献与 SPEC 冻结

G2-4F0 负门控后，已按 failure-driven 原计划核对 sparse
ego-motion-compensated flow，而不是继续调 depth threshold。

本地优先核对：

- SInDSLAM 原始 PDF/PaperNotes；
- NGD-SLAM 原始 PDF/PaperNotes；
- DVI-SLAM 原始 PDF/PaperNotes。

本地没有 FlowFusion 原始 PDF，才使用 primary arXiv fallback。

冻结的方法身份：

```text
[A/S] FlowFusion optical-flow residual principle
      adapted to current ORB features
      + adjacent successful RGB-D frames
      + ORB-SLAM2 initial SE(3)
```

第一版设计：

- 从当前 ORB feature backward LK 到上一成功帧；
- 用上一帧深度和 `Tcw_current_initial * inverse(Tcw_ref_final)` 生成 ego flow；
- residual 为 observed flow 减 ego flow；
- forward LK 只输出 raw FB error，不设动态门槛；
- 有 GT pose 时同时输出 GT residual，诊断初始位姿敏感性；
- 失败/无效/越界保持 no-evidence；
- 缓存在 `GrabImageRGBD()` 的成功 Track 返回后更新；
- 计算仍位于 initial pose 后、`TrackLocalMap()` 前；
- 不增加 PoseOptimization。

同时冻结独立：

```text
agent_rgb_temporal_motion_proxy_v1
```

它只查看 rectified RGB 的固定 `t-2..t+2` temporal clip 和已有 RGB-only
粗框，输出 moving/stationary/uncertain/occluded proxy；不得读取任何
depth/flow/geometry residual。它不是 GT，但避免用待测 flow residual 给自身
生成标签，也不需要用户逐帧手工标注。

当前仅完成文献审计和实现前 SPEC，尚未写 G2-4F1 代码。

冻结不变量：

```text
dynamic_decision          = none
depth_flow_fusion         = none
direct_slam_state_mutation = none
G1-F / G1-D               = locked
strict hold-out           = sealed and unopened
```

文档：

- `results/g2_4f_2026-07-29/G2_4F1_SPARSE_EGO_FLOW_LITERATURE_AUDIT.md`
- `results/g2_4f_2026-07-29/G2_4F1_SPARSE_EGO_FLOW_SHADOW_SPEC.md`

## 36. G2-4D/F0 Box 粗框时序复核修正

准备独立 RGB 时序代理时，发现 nonobstructing frame 246/478/537 的 v1
粗框分别覆盖背景板或空地面。已只修正这三行，保留其余 45 行及全部历史
审计目录，并重新执行：

- G2-4D semantic box coverage；
- G2-4E exact partition/box-region join；
- G2-4F0 feature inside/outside audit；
- 五帧 RGB temporal review export。

一致性：

```text
G2-4D candidates                  = 48/48
corrected bbox rows               = 3
G2-4E partition exact match       = 42/42
G2-4E partition mismatch          = 0
G2-4F0 available feature frames   = 42
temporal center C++/Python match  = 48/48
```

nonobstructing 无人物 18 帧的框内 positive-presence 均值从 `5.402%`
修正为 `7.866%`，但中位数仅从 `1.632%` 变为 `1.746%`；框外中位数仍为
`6.703%`，presence enrichment 中位数为 `0.289x`。frame 478/537 的真实
箱体位置有较强正证据，但独立 RGB 时序代理将两者标为 stationary/high，
不能作为动态检出成功。

所以数值已纠正，G2-4E/F0 的负门控不变。详细记录：

- `results/g2_4f_2026-07-29/G2_4D_TO_F0_BBOX_TEMPORAL_CORRECTION_RESULT.md`

## 37. G2-4F1 独立 RGB 时序运动代理

在查看任何 G2-4F1 flow residual 之前，已由 Agent 对 48 个 development
候选生成相同 rectified domain 的五帧 RGB-only 时间窗口。中心帧与 C++ 导出
`48/48` byte-exact；标签不读取 depth、geometry、flow 或 proxy 排名。

保守代理分布：

| 标签 | 数量 |
| --- | ---: |
| stationary | 30 |
| moving | 7 |
| uncertain | 2 |
| not_visible | 9 |

这不是 GT。它只用于后续连续 residual 的 development 分层，不能用于直接
选择通用动态阈值，也不能据此进入 G1。

当前下一步已收敛为：

> 按冻结 SPEC 实现 G2-4F1 sparse ego-motion-compensated LK residual
> shadow instrumentation；不融合、不分类、不过滤、不解封 hold-out。

详细记录：

- `results/g2_4f_2026-07-29/G2_4F1_RGB_TEMPORAL_MOTION_PROXY_RESULT.md`

## 38. G2-4F1 Sparse Ego-flow Shadow 实现与实验

已按冻结 SPEC 实现相邻成功帧稀疏 residual：

```text
current ORB feature
→ backward/forward LK
→ reference depth + initial/final adjacent pose
→ observed flow - camera-induced ego flow
```

实现保持只读，不设 dynamic threshold，不与 depth vote 融合。确定性测试和
CSV invariant 全部通过。

Bonn GT 诊断还修正了一个真实坐标链遗漏：官方
`T_ROS * T_m` 现在作为可选 `GroundTruth.TwcRightTransform` 仅在 Bonn F1
配置启用。GT residual 明显下降，但仍有同步/标定不确定性，因此不参与判决。

两条完整 online CUDA semantic 结果：

| 序列 | 帧 | actual FPS | mask age | F1 active 中位 |
| --- | ---: | ---: | ---: | ---: |
| nonobstructing | 778 | 29.270 | 0/0 | 2.550 ms |
| obstructing | 589 | 29.698 | 0/0 | 2.523 ms |

连续 evidence：

```text
stationary + person absent (30 frames): frame median 0.313 px
moving + person present (6 frames):     frame median 1.334 px
moving + person absent (1 frame):       frame median 0.501 px
```

第一组 moving 对 stationary 的 rank AUC 为 0.917，但与 person-present、
development proxy 选帧和粗框混杂。关键 `moving+person-absent` 只有 1 帧，
科学门不可评价，不能进入 G1。

详细记录：

- `results/g2_4f_2026-07-29/G2_4F1_SPARSE_EGO_FLOW_SHADOW_RESULT.md`

## 39. G2-4F1 独立候选扩充

为确认主门样本不足是否来自原选帧遗漏，新增 RGB/semantic-only 扩充。它不
读取 depth、geometry、flow、SLAM trajectory 或 residual。

全序列 manifest-only CUDA semantic 统计：

```text
nonobstructing person-absent center = 571/778
obstructing person-absent center    = 417/589
```

按 person-absence、RGB temporal change、semantic transition 和均匀时间采样
固定规则，额外审查 50 个五帧 RGB 窗口：

```text
stationary = 48
moving     = 0
uncertain  = 2
```

两个 uncertain 片段的箱体几乎填满视野，无法从 RGB 窗口区分 camera motion
和独立 object motion，因此没有事后改成 positive。

结论：当前两条开发序列不足以独立评价 `moving+person-absent` 门。下一步
需要新的、非 strict-hold-out、可观察未知对象运动 development 数据；不能
继续在现有样本上调 flow/depth 融合阈值。

详细记录：

- `results/g2_4f1_expansion_2026-07-29/G2_4F1_INDEPENDENT_CANDIDATE_EXPANSION_RESULT.md`

冻结状态：

```text
dynamic_decision           = none
depth_flow_fusion          = none
direct_slam_state_mutation = none
G1-F / G1-D                = locked
strict hold-out            = sealed and unopened
```

## 40. G2-4F1D 非 Holdout 气球开发评价

在打开数据前冻结独立协议，将：

```text
balloon / balloon2          = development screening
balloon_tracking            = strict holdout，继续封存
```

候选选择只读取 rectified RGB temporal clip 和同步 C++ person mask，不读取
任何 geometry/flow residual。低分辨率联系表误将黄色推车轮当作气球后，又由
exact 640×480 中心图完成修正，没有保留错误正样本。

两条完整 online CUDA semantic + G2-4F1 shadow 已运行。exact-zero
person-mask-overlap 的 moving-observable 子集共有 7 帧，其中 6 帧有可测
框内 ORB：

```text
框内 residual 中位       = 11.919 px
同帧去人物背景中位        = 0.723 px
框内减框外中位            = 11.280 px
框内/框外中位             = 20.045x
GT-pose 框内中位          = 11.582 px
```

这是明确的 development directional evidence，说明 sparse ego-flow residual
在语义未覆盖的独立运动气球上值得继续；它仍不是动态分类器，也没有选择阈值。

运行健康度：

| 序列 | FPS | active中位 | F1中位 | ATE RMSE | RPE RMSE |
| --- | ---: | ---: | ---: | ---: | ---: |
| balloon | 29.498 | 32.543 ms | 2.544 ms | 0.031321 m | 0.020778 m |
| balloon2 | 28.277 | 35.309 ms | 2.691 ms | 0.056248 m | 0.032343 m |

ATE/RPE 使用 20 ms 同步，只是 shadow 运行健康度，不是几何改善量。

当前决策：

```text
G2-4F1 directional development evidence = PASS
dynamic threshold                       = none
dynamic decision                        = none
G1-F / G1-D                             = locked
strict holdout                          = sealed and unopened
```

下一步先做有本地文献依据的可靠 feature gate 与静态风险协议，不直接把当前
residual 中位数变成阈值。

详细记录：

- `results/g2_4f1_development_data_2026-07-29/G2_4F1D_NON_HOLDOUT_DEVELOPMENT_DATA_PROTOCOL.md`
- `results/g2_4f1_development_data_2026-07-29/G2_4F1D_BALLOON_DEVELOPMENT_EVALUATION_RESULT.md`

## 41. G2-4F2 可靠 Feature Candidate 与跨域静态风险

先从本地 PaperNotes、原始 PDF 和 DynaSLAM 官方源码核对：

- FlowFusion：observed flow 减 camera ego-flow；
- Kalal：forward-backward error 只用于 correspondence reliability；
- Li & Lee：零中心鲁棒尺度和 Student-t 连续静态权重；
- DynaSLAM：depth patch 高方差用于抑制 moving-border 误判；
- SInDSLAM：relative+absolute depth-discontinuity 定义。

归属边界已经冻结：当前稀疏 ORB/LK 版本均为 `[A/S/H]` 适配，不是上述系统
复现；论文参数不能直接迁移。

两个真正静态域的 150 帧短测完成：

| 冻结诊断点 | TUM fr1/xyz | Bonn static_close_far |
| --- | ---: | ---: |
| `FB<=0.25, q>=10` candidate rate | 0.232% | 0.546% |
| MapPoint candidate rate | 0.068% | 0.117% |
| candidate/frame median | 0 | 3 |
| candidate/frame max | 25 | 21 |

新增参考深度 boundary/invalid 1–2 pixel 风险分层后发现，Bonn `q=10` 候选中
75.2% 仍处于 `clean_d2`；depth boundary 本身只承载 1.7%。因此边界主解释
和 boundary veto 均被否决。

GT-pose 只读诊断的 `q=10` 候选率为：

```text
TUM  = 0.069%
Bonn = 0.032%
```

它支持“SLAM-pose 分支与静态高 q 尾部有关”的有限判断，但 GT raw residual
反而更大，不能宣称 GT raw projection 更准确或 pose 是唯一原因。

在预先冻结的网格中，`FB<=0.25, q>=10` 是最保守且仍保留 development
气球 6/6 可测帧框内信号的 Pareto 点，现冻结为：

```text
high_residual_candidate
```

不是 `dynamic=true`。strict holdout 协议也已在打开数据前写入：

- 至少 5 个可测 moving exact-zero-person proxy frame；
- 至少 80% 有框内 candidate；
- 至少 80% 的框内 candidate rate 高于同帧框外；
- 全序列 MapPoint candidate rate 不超过 0.20%；
- 任何情况下继续保持 shadow-only。

当前状态：

```text
G2-4F2Q quality/robust scale       = PASS
G2-4F2B boundary-risk diagnosis   = PASS, main explanation REJECTED
G2-4F2D candidate working point   = FROZEN
strict holdout                    = SEALED, ready for one-time run
G1-F / G1-D                       = LOCKED
```

详细记录：

- `results/g2_4f2_2026-07-30/G2_4F2_RELIABLE_FEATURE_GATE_LITERATURE_AUDIT.md`
- `results/g2_4f2_2026-07-30/G2_4F2_RELIABLE_FEATURE_GATE_SHADOW_SPEC.md`
- `results/g2_4f2_2026-07-30/G2_4F2B_DEPTH_BOUNDARY_RISK_SHADOW_SPEC.md`
- `results/g2_4f2_2026-07-30/G2_4F2B_DEPTH_BOUNDARY_RISK_SHADOW_RESULT.md`
- `results/g2_4f2_2026-07-30/G2_4F2D_CANDIDATE_WORKING_POINT_FREEZE_AND_HOLDOUT_PROTOCOL.md`

## 42. G2-4F2H Strict Holdout 与 F2 冻结结论

`balloon_tracking` 已按解封前协议完成唯一一次正式 semantic+F1 shadow
运行。先于 geometry 冻结的 RGB-only temporal proxy 共 14 帧，14/14 的
同步 C++ person mask 均为全零；代理不是 GT。

冻结 `FB<=0.25,q>=10` 的结果：

```text
measurable moving exact-zero proxy          = 14/14
frames with in-box candidate                = 7/14 = 50%
frames with inside rate > outside rate      = 7/14 = 50%
full-sequence MapPoint candidate rate       = 0.2248%
```

相对预冻结的 `80%/80%/<=0.20%` 门全部未通过。因此：

```text
G2-4F2 hard candidate gate = FAILED
threshold retuning         = forbidden on holdout
G1-F0 / G1-F / G1-D        = locked
```

失败不等于连续证据完全无效。SLAM-pose 连续 residual 的框内中位高于同帧
框外为 13/14，GT-pose 为 12/14；框内/框外中位比约 9.98。准确结论是：

> continuous sparse ego-flow inconsistency 通常局部富集，但当前全帧尺度上的
> `q>=10` hard candidate 不够稳定。

shadow 轨迹健康度（40 ms GT 配对）：

```text
ATE RMSE = 0.031290 m, 590 pairs
RPE RMSE = 0.024986 m, 589 pairs
```

无 mutation baseline，不能解释成 geometry 改善。

首次完整运行暴露诊断容器每帧精确 `reserve(size+samples)` 的 O(N²) 搬移，
导致 F1 active time 从约 2.8 ms 增长到约 26 ms、FPS 仅 24.00。删除该
`reserve` 后，只在非 holdout Bonn static 150 帧验证：F1 始终约
2.7–2.9 ms，29.72 FPS，0/150 deadline miss。未重跑 holdout；因此原
residual 科学结果保留，原 24 FPS 只作为无效性能测量记录。

详细记录：

- `results/g2_4f2_holdout_2026-07-30/G2_4F2H_RGB_ONLY_PROXY_FREEZE.md`
- `results/g2_4f2_holdout_2026-07-30/G2_4F2H_STRICT_HOLDOUT_RESULT.md`

## 43. 下一步回到原定 failure-driven 刚性验证

没有通过降低 `q` 修补 F2。先核对本地 SInDSLAM、DetectFusion 笔记；本地
缺少 Dai 等 point-correlation 原文，才使用 arXiv primary fallback。

下一阶段冻结为：

```text
G2-4F3 local rigidity coherence shadow
```

文献边界：

- `[L]` Dai 等以 Delaunay map-point graph 和跨时相对位置一致性分组刚体；
- `[L]` SInDSLAM/DetectFusion 支持 residual 需要空间区域上下文；
- `[A]` 本项目只适配为相邻帧 ORB/RGB-D 局部 feature graph；
- `[S/H]` “违反静态 ego-motion 且内部刚性保持”是否可分仍需实验；
- 不实现长期 graph optimization，不假设最大组静态，不修改后端。

先写 SPEC 和连续 edge/component 统计，再决定是否值得实现。详细决策：

- `results/g2_4f2_holdout_2026-07-30/G2_4F3_LOCAL_RIGIDITY_LITERATURE_DECISION.md`
- `results/g2_4f3_2026-07-30/G2_4F3_LOCAL_RIGIDITY_COHERENCE_SHADOW_SPEC.md`

当前只冻结了实现前 SPEC，尚未写 F3 代码。第一版禁止选择刚性阈值或建立带
语义的 `rigid/dynamic component`，只输出 edge strain 与 residual 的连续联合
统计。

## 44. G2-4F3 局部刚性一致性 Shadow 已完成第一版

已按冻结 SPEC 实现：

```text
F1 相邻帧 LK/ego-flow
→ semantic exclusion + FB<=0.25
→ 两帧 RGB-D 三维点
→ current-image Delaunay graph
→ absolute/relative edge strain
→ node median/P90 与 edge/node/frame CSV
```

文献身份保持为 Dai 等 point-correlation 的局部两帧适配 `[A/H]`，并与
FlowFusion 启发的 sparse residual 分开记录；不是任何原系统复现。

确定性测试、OpenCV 4.5.4 C++/Python 2526 条边逐集合一致性、Bonn/TUM 两个
真静态 150 帧审计均通过。加入 `1e-3 px` 数值近重复点去重后，F3 计算中位
约 `1.90–1.98 ms`，P95 `2.10–2.11 ms`；该容差不是运动阈值。

冻结的 `balloon/balloon2` development 粗框代理中：

```text
框内 flow median > 框外：15/15 可比较帧
内部 strain median <= 背景：11/14 可比较帧
跨组 strain median > 背景：13/14 可比较帧
```

这支持“静态模型不一致 + 内部近似刚性”的方向性假设，但仍有节点不足、深度
混合和 relative-strain 长边归一化失真。当前状态：

```text
dynamic_decision             = none
direct_slam_state_mutation   = none
rigidity threshold/component = none
G1-F / G1-D                  = locked
```

下一步先核对文献并设计鲁棒连续 edge reliability；不能直接选阈值，也不能
使用已经打开的 `balloon_tracking` 调参。

详细记录：

- `results/g2_4f3_2026-07-30/G2_4F3_LOCAL_RIGIDITY_COHERENCE_SHADOW_RESULT.md`

## 45. G2-4F3U 不确定度归一化边长应变已完成并否决为默认分数

本阶段先补齐本地缺失的原始文献证据，再实现 shadow-only 连续诊断：

```text
Dai point-correlation covariance principle [A]
+ Khoshelham Kinect sigma_z proportional to z^2 [L/A]
+ 3x3 valid-depth mixture uncertainty [S]
+ axial uncertainty to edge-length first-order propagation [S]
→ uncertainty-normalized two-frame scalar edge strain [S/H]
```

工程实现与确定性测试通过；没有动态阈值、动态 component 或 SLAM mutation。
真静态短序列中新增 metric 约 `0.53–0.58 ms` median，所有 131256 条静态边
均未触发分母 floor。

但冻结 development proxy 的核心结果为：

```text
normalized internal <= background: 4/14
absolute internal <= background:   11/14
normalized crossing > background:  13/14
```

所以“不确定度归一化能改善运动刚体内部一致性”的假设未通过。保留 measurement
与负结果，否决其作为默认 coherence score；不选择 hard threshold，G1-F/G1-D
继续锁定。

在线 Bonn exact semantic 运行还显示完整同步 pipeline 为 `28.40 FPS`
（balloon）和 `25.99 FPS`（balloon2）。F3U 相对旧 F3 只增加约
`0.16–0.20 ms` median，不能把全部 deadline miss 归因于该子模块，但当前
Bonn 系统没有稳定 30 FPS 余量。

详细记录：

- `results/g2_4f3u_2026-07-30/G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_LITERATURE_AUDIT.md`
- `results/g2_4f3u_2026-07-30/G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_SHADOW_SPEC.md`
- `results/g2_4f3u_2026-07-30/G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_SHADOW_RESULT.md`

## 46. G2-4F4 区域上下文连续残差已完成并停止

F3U 结束后，先核对本地 DetectFusion/SInDSLAM 笔记和原始 PDF，再冻结一个
最小离线实验：

```text
exact F1 continuous residual
+ G2-3R0 depth-discontinuity regions
→ region median/P90/MAD/support
→ frozen RGB-only coarse-box paired audit
```

没有修改 Tracking 或几何核心源码，只新增只读工具和文档。17 个粗框帧中，
14 帧有至少 3 个 selected-region features：

```text
point inside > background        14/14
selected region > background     11/14 = 78.57%
预冻结要求                       >=80%
```

部分 depth component 横跨大块背景，使连续 F1 信号被整区 median 稀释。
因此按停止条件否决当前区域容器，不修改 selector，不调阈值，不做 online F4，
G1-F/G1-D 继续锁定。

当前不是“F1 没有效果”：同帧点级粗框信号仍为 14/14。失败的是简单深度连通
区域作为运动对象单位。下一步必须在更完整的文献 re-clustering 与保守的
feature-level 路线之间做总决策。

详细记录：

- `results/g2_4f4_2026-07-30/G2_4F4_REGION_CONTEXT_LITERATURE_DECISION.md`
- `results/g2_4f4_2026-07-30/G2_4F4_REGION_CONTEXT_SHADOW_SPEC.md`
- `results/g2_4f4_2026-07-30/G2_4F4_REGION_CONTEXT_SHADOW_RESULT.md`

## 47. 运动分组输入审计与 G2-4R1 路线冻结

F4 停止后没有立即发明新的 grouping threshold。先审计现有 F1/F3 输入：

```text
代理内 MapPoint:
  balloon   8/76  = 10.53%
  balloon2  4/122 = 3.28%

三图像轨迹仍有 >=3 nodes:
  balloon   4/8 frames
  balloon2  4/9 frames

六图像轨迹仍有 >=3 nodes:
  balloon   2/8 frames
  balloon2  2/9 frames
```

因此固定 3/5 帧 persistence 和基于已有 MapPoint 的长轨迹聚类均未获批准。
两帧 transient LK grouping 只具备部分输入，并且不能输出低纹理 `M_depth`。

随后核对 Dai point correlations、Tateno incremental segmentation、
DetectFusion 和 Nguyen Kinect noise model 原文，并与当前代码逐项比较。

路线决策：

```text
Dai-style transient-LK point correlation  = 延期
G2-4R1 normal+distance frame segmentation = 批准离线 representation shadow
dynamic decision                          = none
direct SLAM mutation                       = none
G1-F / G1-D                                = locked
```

G2-4R1 只实现 Tateno-style 帧级 vertex/normal、concavity edge、
noise-aware point-to-plane distance edge 和 connected components；不实现
Tateno 全局 label propagation/map update，也不复制 DetectFusion 的 surfel/ICP
motion mask。

详细记录：

- `results/g2_4_motion_grouping_2026-07-30/G2_4_MOTION_GROUPING_INPUT_FEASIBILITY_RESULT.md`
- `results/g2_4_motion_grouping_2026-07-30/G2_4_MOTION_GROUPING_ROUTE_DECISION.md`
- `results/g2_4_motion_grouping_2026-07-30/G2_4R1_NORMAL_DISTANCE_SEGMENTATION_SHADOW_SPEC.md`

## 48. G2-4R1 法向量＋距离分段 Shadow 已完成并停止

按冻结 SPEC 新增离线只读工具：

```text
DT-SLAM/tools/audit_normal_distance_segments.py
```

正式实验纠正为读取完整 F1 `measured` sparse ego-flow CSV，而不是带 F3U
刚性邻域条件的 node 子集。segment 只由 rectified metric depth、normal 和
point-to-plane distance 生成；bbox/residual 只用于分段后的 development
oracle review。

17 个 Bonn balloon/balloon2 候选帧中：

```text
normal region count median       2487 / 2669
<=64 px region fraction median   94.51% / 94.98%
single segment bbox coverage     0.319 / 0.609 median
all segments reach 80% bbox      1 / 17 frames
selected residual > background   14 / 14 comparable
```

TUM fr1/xyz 真静态 150 帧仍有 `406` 个区域中位、`93.38%` 小区域比例中位。
说明 normal+distance 确实切开了大背景 component，但当前固定参数适配严重
过分割，无法提供稳定整对象容器。Python 离线时延中位约 `195–215 ms`，不等价
于 C++ 在线性能。

按预注册停止条件：

```text
G2-4R1 frame-wise representation = frozen negative result
parameter retuning / merge repair = not allowed on opened proxy
dynamic_decision / mutation       = none / none
G1-F / G1-D                       = locked
```

该结论不否定 Tateno 的完整全局 label propagation/merging，也不否定
DetectFusion 的 surfel-map/ICP motion pipeline；两者均未复现。

详细记录：

- `results/g2_4r1_2026-07-30/G2_4R1_NORMAL_DISTANCE_SEGMENTATION_SHADOW_RESULT.md`

## 49. G2-4R2 静态标定图分割 Shadow 已完成并停止

R1 后没有立即实现完整 Dai。先纠正“目标内 MapPoint 少即可否定图路线”的
过强推论，再用现有 F3/F3U 原始节点补测：

```text
重新 Delaunay
→ 真静态前半段 q90–q99.5 标定
→ scalar strain 异常边删除
→ connected components
→ 冻结动态 bbox proxy 富集
```

all-transient q90 在静态 validation 的 outside-primary P95 为 `2.82%`，
但 14 个支持帧的 bbox recall 中位仅 `9.69%`，`0/14` 达到 50%；MapPoint-
only 只有 `1/17` 帧具备至少 3 个 bbox nodes，且没有隔离出该点。所有预注册
工作点均失败。

图重建与原 C++ F3 edge-set 在三套数据上逐边一致，重复运行字节一致。结果
因此冻结为：

```text
scalar strain + quantile + CC = stopped negative adaptation
Dai full point correlation    = not implemented, not rejected
dynamic decision / mutation   = none / none
G1-F / G1-D                   = locked
```

不能再在这些开发代理上调 edge threshold。下一步先比较更完整文献子集、机会式
高精度 F1 复核和补充受控数据三条路线，再决定是否继续实现。

详细记录：

- `results/g2_4r2_2026-07-30/G2_4R2_STATIC_CALIBRATED_GRAPH_PARTITION_SHADOW_RESULT.md`

### 49.1 下一路线

下一步不再追加图阈值，先冻结 G2-5A 自动代理评价 SPEC：

```text
semantic-blind F1 residual
+ offline semantic reference proxy
→ feature-level separability / high-precision feasibility
```

semantic 不进入几何算法，只用于自动评价，避免用户逐像素人工标注。若该阶段
仍不能形成跨序列、低静态风险的高精度工作点，则暂停轻量 G1-F 路线，不再
无限增加 shadow 修补。

## 50. G2-5A Semantic-reference F1 连续证据审计通过

本阶段未修改 C++：F1 原实现已经对全部 ORB feature 计算 residual，
`semantic_nonzero` 只在导出时作为事后标签。

TUM walking/sitting 各运行 149 帧在线同步 CUDA semantic，mask age 均为
`median=0,max=0`。离线逐帧结果：

```text
walking:
  comparable 113
  semantic median > background 95.58%
  AUC median 0.857

sitting:
  comparable 144
  semantic median > background 79.17%
  AUC median 0.659
```

结合 frozen unknown balloon development 的 `15/15` 方向性，G2-5A 的
continuous evidence 条件通过。后续尺度身份复核确认：raw residual 不读取
semantic；初版 q scale 排除了 semantic feature，不能称为 semantic-blind。
权威 v2 已并列输出 `q_blind`（全部合格 feature 估计尺度）和
`q_combined`（排除 semantic feature）。连续方向/AUC 结论不变。它仍只证明
F1 连续分数有跨类别可分性；q10 hard gate 的旧 holdout 失败仍有效。

下一步允许设计 G1-F0：

```text
q=6/8/10 counterfactual MapPoint deletion audit
```

只统计假设删除后的观测支持，不修改 `mvpMapPoints`，不增加优化调用。

## 51. G1-F0A 初始 MapPoint 关联反事实已完成

新增只读工具：

```text
DT-SLAM/tools/audit_initial_mappoint_counterfactual.py
```

它在 F1 当前调用位置的 CSV 上，对 `q=6/8/10` 统计已有 MapPoint 的假设删除，
同时运行 semantic-blind 与 combined 两种 frame scale。六组数据上：

```text
baseline>=10 → remaining<10      全部 0 帧
跨 10/15/20/30/50 支持线         全部 0 帧
fr1 static q6 MP candidate       0.0721%
Bonn static q6 MP candidate      0.1831%
```

两个真静态域均低于预冻结 0.20% 工程预算，且两种 scale identity 的支持结论
一致。G1-F0A 因此通过“初始关联支持可行性”。

但 unknown balloon 上实际候选非常少：

```text
balloon  q6/q8/q10 = 2/0/0
balloon2 q6/q8/q10 = 3/2/1
```

所以这不是动态检测有效性或 ATE 改善证据。下一步是 G1-F0B：
在 `SearchLocalPoints()` 后进行默认关闭的在线反事实统计，不实际清除
`mvpMapPoints`，不增加 PoseOptimization。

```text
G1-F0A initial support       PASS
G1-F0B design               ALLOWED
G1-F / G1-D                 LOCKED
dynamic decision / mutation none / none
```

详细记录：

- `results/g1_f0_2026-07-30/G1_F0A_INITIAL_ASSOCIATION_COUNTERFACTUAL_SPEC.md`
- `results/g1_f0_2026-07-30/G1_F0A_INITIAL_ASSOCIATION_COUNTERFACTUAL_RESULT.md`

## 52. G1-F0B `SearchLocalPoints()` 后反事实已完成并失败

新增默认关闭的 C++ raw association snapshot 和离线工具：

```text
DT-SLAM/tools/audit_post_search_mappoint_counterfactual.py
```

同次运行的 F1 与 snapshot 在六序列上满足 feature-index、semantic flag 和
exact counted inlier 不变量。第一版重新读取 MapPoint 状态推导 inlier 时发现
与 Tracking 计数不一致；v2 改为在原 `mnMatchesInliers` 循环同步记录
`counted_tracking_inlier`，没有放宽检查。

post-pose 虽然没有任何 30/50 支持线 crossing，但 Bonn 真静态：

```text
q6   0.3699%
q8   0.2680%
q10  0.2237%
预算 <=0.20%
```

而 unknown development 的实际作用有限：

```text
balloon   0/0/0 MapPoints
balloon2  6/2/2 MapPoints
```

因此 q6/q8/q10、两种 scale mode 均未通过预注册条件。禁止继续提高 q 或放宽
预算来适配已打开数据。

```text
G1-F0B                       FAIL
q selection                  none
G1-F / G1-D                  LOCKED
dynamic decision / mutation  none / none
下一步                       总路线书面决策
```

详细记录：

- `results/g1_f0_2026-07-30/G1_F0B_POST_LOCAL_SEARCH_COUNTERFACTUAL_SPEC.md`
- `results/g1_f0_2026-07-30/G1_F0B_POST_LOCAL_SEARCH_COUNTERFACTUAL_RESULT.md`

## 53. G1-F0 后路线决策

当前停止 single-feature q threshold 微调。下一步优先补独立 unknown-object
评价依据，而不是立即扩大 detector：

```text
G2-6E evaluation-only feasibility:
Bonn official static environment model
+ official camera pose
+ current depth
- online person mask
→ unknown foreground review proxy
```

该 proxy 只用于离线评价，不能接入 detector，也不能称 pixel GT。当前本地没有
找到官方 static model 文件，需先从官方来源补齐并冻结坐标链/静态风险审计。

若该独立 proxy 仍显示 F1 unknown signal 弱，则停止 lightweight feature
filter 路线；只有论文明确需要 geometry 时，才另立 StaticFusion/DetectFusion
类区域概率或 Jaimez/FlowFusion 类多运动分组的较重分支。

详细记录：

- `results/g1_f0_2026-07-30/G1_F0_POST_COUNTERFACTUAL_ROUTE_DECISION.md`

## 54. G2-6E Bonn 静态模型评价可行性已完成并在 E1 停止

按照 G1-F0 后路线决策，先冻结 evaluation-only SPEC，再从 Bonn 官方来源取得
1 mm subsampled static point cloud、官方坐标脚本和 ReFusion 论文。

新增：

```text
DT-SLAM/tools/prepare_bonn_static_model.py
DT-SLAM/tools/audit_bonn_static_model_alignment.py
```

54,676,774 点 ASCII PLY 没有完整解压；流式生成 stride16（3,417,299 点）和
stride8（6,834,597 点）紧凑模型，并保存来源/输出 SHA-256。

静态投影方向检查确认：

```text
inverse(T_g): median residual 0.035 m
direct T_g:   median residual 2.102 m
```

所以基本正逆方向正确。但 34 帧全序列静态消融显示：

| model | splat | coverage median | non-risk median | non-risk `r>0.1m` |
|---|---:|---:|---:|---:|
| stride16 | 0 | 65.55% | 0.127 m | 56.23% |
| stride16 | 1 | 90.54% | 0.065 m | 42.60% |
| stride16 | 2 | 98.53% | 0.042 m | 37.75% |
| stride8 | 1 | 97.35% | 0.055 m | 40.53% |

若干具有大量有效深度的静态帧仍有 `0.10–0.44 m` non-risk median 和
`50%–94%` 正残差比例。更密点云和更大 splat 不能解决。

当前 scanner model inconsistency 混合静止但不在 scanner static model 中的
物体、模型遮挡/采样、坐标尺度误差和深度噪声，不能直接作为逐像素
unknown-motion proxy。按预冻结规则停止：

```text
G2-6E1 static alignment      FAIL
G2-6E2 moving proxy          NOT RUN
G2-6E3 F1 join               NOT RUN
G1-F / G1-D                  LOCKED
SLAM mutation                NONE
```

没有以 moving-box 数据调 offset/阈值，也没有扩张成 ICP、TSDF 或 ReFusion
复现。

详细记录：

- `results/g2_6e_2026-07-31/G2_6E_BONN_STATIC_MODEL_UNKNOWN_FOREGROUND_EVALUATION_SPEC.md`
- `results/g2_6e_2026-07-31/G2_6E_BONN_STATIC_MODEL_ALIGNMENT_RESULT.md`

## 55. G2-6O Bonn 箱子运动可观测性审计完成，未补齐独立动态样本

G2-6E static-model proxy 失败后，按路线决策先扩充三条官方 Bonn
development/review 序列：

```text
placing_nonobstructing_box
removing_nonobstructing_box
kidnapping_box
```

association 工具新增 ZIP 直读，三序列得到 `720/494/1091` 个一对一 RGB-D
pair。新增只读工具：

```text
DT-SLAM/tools/prepare_bonn_box_motion_observability_review.py
```

候选只由 uniform 与 coarse RGB temporal change 高/低三层组成，不读取
geometry、flow、depth residual 或 SLAM 结果。每序列 18 个，总计 54 个。

初版 0.4 秒联系表不足以区分 `kidnapping` 的相机视差和对象运动；没有重选
候选，第二版 exact reuse 相同候选并扩到约 2 秒显示跨度。Agent RGB-only
粗审结果：

```text
placing:
  moving/transition 均有 carrier
  moving + person absent = 0

removing:
  moving/transition 均有 carrier
  moving + person absent = 0

kidnapping:
  person absent
  12/18 因强相机运动、多箱体切换而保持 uncertain
  moving + person absent + confidence>=medium = 0
```

这不是“序列没有动态”的结论，而是当前 RGB-only 非 GT 粗审不能提供低混杂的
unknown-box reference。结合既有 G2-4F1 两条 moving-box 序列只有 1 个
`moving + person absent` 候选，本次扩展没有补齐评价缺口。

```text
G2-6O qualification            FAIL
G2-6F new evidence audit       NOT RUN
G1-F / G1-D                    LOCKED
dynamic decision / mutation    none / none
```

下一步回到预先写明的分叉：优先采集受控、操作者不入镜的 RGB-D 箱子序列；
若没有传感器，则 motion grouping 只能作为 development-only shadow
feasibility，不能解锁过滤或宣称 unknown-object precision/recall。

详细记录：

- `results/g2_6_box_data_2026-07-31/G2_6O_BONN_BOX_MOTION_OBSERVABILITY_AUDIT_SPEC.md`
- `results/g2_6_box_data_2026-07-31/G2_6O_BONN_BOX_MOTION_OBSERVABILITY_AUDIT_RESULT.md`

## 56. G1-F1 首次进入真实 tracking association 过滤

用户明确修订工程判据：几何方法不必达到近乎零假阳性；允许少量静态背景关联
被删，只要 ATE/RPE、轨迹覆盖和运行速度不发生明显退化。原 G1-F0 的
`<=0.20%` counterfactual 静态预算因此保留为历史诊断，不再作为绝对阻断条件。

G1-F1 复用已实现的 G2-4F1：

```text
双向稀疏 LK observed flow
- RGB-D/SE(3) ego flow
→ 连续残差
→ semantic-blind 帧尺度
→ q6/q8/q10 高残差候选
```

过滤只发生于：

```text
TrackLocalMap::SearchLocalPoints
→ 既有 semantic association removal
→ 清除少量候选 mvpMapPoints
→ 第二次既有 PoseOptimization
```

没有第三次优化，不写 `mvbDynamic`，不禁止 MapPoint 创建，不修改
Optimizer/g2o/YOLO。若尺度无效、位于重定位保护窗口、关联不足、删除后不足
30 或候选超过 5%，整帧 fail-open。

结果：

```text
fr1/xyz:
  q6/q8/q10 ATE/RPE 相对 control 变化约 -0.2% 到 +1.7%
  792/792 轨迹均完整

fr3/walking semantic+geometry，三轮中位数:
  semantic control ATE 0.017699 m
  q6               ATE 0.014927 m  (-15.66%)
  q8               ATE 0.016116 m  ( -8.94%)
  q10              ATE 0.015462 m  (-12.64%)
  RPE 改善约 1.95%–2.80%
  FPS 代价约 0.45%–0.52%
  12/12 次轨迹均完整
```

41 个 filter CSV、26,928 个 frame row 的审计无不变量违反：

```text
pose_reoptimization = none
mapping_veto        = none
```

当前结论：

```text
G1-F1 experimental tracking filter   KEEP, default OFF
experimental working point           q10 (conservative)
unknown-box effectiveness            NOT ESTABLISHED
G1-M / G1-D                          LOCKED
```

`fr3/sitting_static` 三轮也已完成，12/12 条轨迹完整。相对 semantic control，
q6/q8/q10 的 ATE 中位变化分别为 `-2.10%/+3.82%/-3.37%`，均未达到约
10% 工程退化线。

Bonn balloon 三轮也已完成，12/12 条轨迹完整。q6/q8/q10 的 ATE 中位改善约
`5.4%/6.4%/1.8%`，RPE 中位轻微恶化约 `2.1%/0.7%/0.2%`。基于三条
semantic 序列均无明显 ATE 退化、删除量最低和 Bonn RPE 变化最小，冻结 q10
为后续保守实验工作点；默认开关仍关闭，q6/q8 保留为消融。

下一步进入 G1-M0 MapPoint 写入 counterfactual，只统计不修改地图。不得把
ATE 改善包装成未知箱子检测已经解决。

详细记录：

- `results/g1_f1_2026-07-31/G1_F1_SPARSE_EGO_FLOW_TRACKING_FILTER_SPEC.md`
- `results/g1_f1_2026-07-31/G1_F1_SPARSE_EGO_FLOW_TRACKING_FILTER_RESULT.md`
- `results/g1_f1_2026-07-31/G1_F1_FORMAL_METRICS.csv`
- `results/g1_f1_2026-07-31/filter_invariant_audit.json`

## 57. G1-M0 MapPoint 写入 counterfactual 完成

G1-F1 只清除 tracking association，不能自动保护地图。代码审计发现：

```text
TrackLocalMap 中清除 q10 mvpMapPoints
→ 当前帧被选为 KeyFrame
→ CreateNewKeyFrame 看到“无 MapPoint + 有有效深度”
→ 同一 feature 重新 new MapPoint
```

新增默认关闭的 G1-M0，只记录：

- RGB-D 初始化写图；
- `CreateNewKeyFrame()` 中的候选暴露和即时 MapPoint 创建；
- G1-F1 删除后又重新创建的数量。

没有设置 `mvbDynamic`，没有阻止 MapPoint，没有修改 LocalMapping、Optimizer、
g2o 或 YOLO。

在线 CUDA semantic + G1-F1 q10，每序列三轮中位数：

| 序列 | 全部创建 MapPoint | q10 candidate 创建 | candidate 比例 | tracking 删除后重建 |
|---|---:|---:|---:|---:|
| walking | 26,583 | 653 | 2.411% | 141 |
| sitting_static | 3,278 | 3 | 0.092% | 0 |
| Bonn balloon | 12,001 | 202 | 1.714% | 25 |

动态 development 序列的候选写图比例相对 sitting 约为 `26.3×/18.7×`。
这不是逐点 GT，但证明 MapPoint admission 非零、可重复且具有动态富集趋势。

10 个 CSV、1,038 个写图事件：

```text
baseline created MapPoints         140,020
candidate created MapPoints          2,503
tracking-removed then recreated        461
invariant violations                     0
counterfactual_only                   true
mapping_veto                          none
```

首帧没有相邻参考，F1 几何不可用；初始化只受语义保护，这是明确限制。

当前决定：

```text
G1-M0                PASS
G1-M1                JUSTIFIED, NOT IMPLEMENTED
G1-M1 default        OFF
G1-D                 LOCKED
```

下一步冻结并实现 G1-M1：只在成功跟踪且将创建 KeyFrame 时，把通过 G1-F1
安全条件的 q10 候选融合到统一 `mvbDynamic`，复用现有
`RemoveDynamicAssociations`、RGB-D admission 检查和 LocalMapping dynamic
guard。不得增加另一套后端状态。

详细记录：

- `results/g1_m0_2026-07-31/G1_M0_MAPPOINT_ADMISSION_COUNTERFACTUAL_SPEC.md`
- `results/g1_m0_2026-07-31/G1_M0_MAPPOINT_ADMISSION_COUNTERFACTUAL_RESULT.md`
- `results/g1_m0_2026-07-31/G1_M0_FORMAL_METRICS.csv`
- `results/g1_m0_2026-07-31/mapping_counterfactual_audit.json`

## 58. G1-M1 MapPoint 写入过滤完成

G1-M1 已完成原始稀疏地图保护目标。最小接入保持为：

```text
CreateNewKeyFrame
→ 复用 G1-F1 q10 与 tracking fail-open 状态
→ feature/depth 候选均不超过 5%
→ 否决后至少保留 100 个有效深度 feature
→ candidate 并入统一 mvbDynamic
→ 既有 RemoveDynamicAssociations
→ 既有 RGB-D !mvbDynamic admission
→ KeyFrame 复制 flag
→ 既有 LocalMapping dynamic endpoint guard
```

没有修改 LocalMapping、Optimizer、g2o、YOLO，也没有增加位姿优化。

四类序列、每类三轮的中位数：

| 序列 | ATE RMSE | RPE RMSE | FPS | 相对 control |
|---|---:|---:|---:|---|
| walking | 0.017864 m | 0.012343 | 27.479 | ATE +0.93%，RPE +0.97% |
| sitting_static | 0.006596 m | 0.005669 | 27.890 | ATE -2.64%，RPE +1.14% |
| Bonn balloon | 0.032519 m | 0.041264 | 29.538 | ATE -0.26%，RPE +0.88% |
| fr1/xyz | 0.009643 m | 0.005777 | 29.624 | ATE -0.66%，RPE +0.35% |

12 次运行全部完整。1,200 个写图事件中：

```text
applied rows                     399
new dynamic flags              2,619
vetoed valid-depth features    2,441
fail-open rows                   103
invariant violations               0
```

所有 applied 行的 candidate-created MapPoint 为 0。首帧继续
`reference_unavailable_fail_open`。

Viewer ON 的 G1-M1 与关闭 G1-F1/G1-M1 的语义对照都在完成全部帧、保存轨迹
后 exit 139；因此对照表明该关闭阶段问题不依赖 G1-M1。本阶段不修改
Viewer/Pangolin，正式指标全部使用 Viewer OFF。

当前状态：

```text
G1-F1 tracking protection             EXPERIMENTALLY USABLE, default OFF
G1-M1 sparse MapPoint protection      EXPERIMENTALLY USABLE, default OFF
ATE improvement claim                 NOT SUPPORTED
dynamic-map cleanliness with GT       NOT YET MEASURED
G1-D depth-region protection          LOCKED / UNSOLVED
```

这意味着稀疏几何前端已形成可用实验版本；后续不能继续调 5% 参数追逐单序列
ATE。下一决策应是地图质量评价，或另立有依据的 G1-D 像素区域路线。

详细记录：

- `results/g1_m1_2026-07-31/G1_M1_MAPPOINT_ADMISSION_FILTER_SPEC.md`
- `results/g1_m1_2026-07-31/G1_M1_MAPPOINT_ADMISSION_FILTER_RESULT.md`
- `results/g1_m1_2026-07-31/G1_M1_FORMAL_METRICS.csv`
- `results/g1_m1_2026-07-31/mapping_filter_audit.json`

## 59. G1 稀疏地图质量只读评价完成

为避免把 ATE 当成地图质量、或把“MapPoint 越少”误当成“地图越干净”，新增
默认关闭的 final-map 生命周期审计。它记录 G1-M0/G1-M1 fail-open 中实际创建
的 q10 candidate MapPoint，并在系统关闭、LocalMapping/LoopClosing 停止后沿
`GetReplaced()` 链检查最终存活状态，同时输出 MapPoint、KeyFrame 和 observation
摘要。评价器只读，不改变 tracking、mapping 或优化。

walking 三轮中位数：

| 指标 | G1-M0 | G1-M1 |
|---|---:|---:|
| ATE | 0.019059 m | 0.016745 m |
| RPE | 0.012335 | 0.012486 |
| FPS | 27.340 | 27.301 |
| final MapPoint | 1208 | 1367 |
| candidate created | 711 | 174 |
| candidate final survivors | 25 | 5 |

M0 中约 96%--98% 的可疑候选会被 ORB-SLAM2 自然剔除，但仍有少量候选通过
replacement 链留在最终地图。M1 能阻止 applied candidate 临时写图；不过两组
运行产生的 KeyFrame/MapPoint 集合不同，candidate 又只是 dynamic-enriched
proxy，因此不能把幸存数差值解释为真实动态点清除量。

fr1/xyz、Bonn balloon 和 sitting 也完成一轮成对检查：全部轨迹完整，未出现
静态地图或 observation 塌缩。fr1 ATE/RPE 变化约 +0.2%/+0.6%；Bonn 单次变化
约 +3.3%/+1.5%；sitting 的 M1 没有 applied event，其轨迹差异只代表运行波动。

当前结论：

```text
temporary MapPoint admission protection    CONFIRMED
static catastrophic degradation            NOT OBSERVED
exact dynamic-map precision/recall         NOT MEASURED
ATE improvement claim                      NOT SUPPORTED
cleaner-map causal claim                   SUGGESTIVE, NOT PROVEN
G1-F1 + G1-M1                              EXPERIMENTALLY USABLE, default OFF
G1-D                                       STILL LOCKED
```

详细记录：

- `results/g1_map_quality_2026-07-31/G1_SPARSE_MAP_QUALITY_EVALUATION_SPEC.md`
- `results/g1_map_quality_2026-07-31/G1_SPARSE_MAP_QUALITY_EVALUATION_RESULT.md`
- `results/g1_map_quality_2026-07-31/G1_MAP_QUALITY_FORMAL_METRICS.csv`
- `DT-SLAM/tools/audit_sparse_map_quality.py`

## 60. G1 稀疏前端四模式首轮冻结

新增单一运行入口，使用同一二进制显式切换：ORB baseline、semantic-only、
geometry-only、semantic+geometry。首轮限定在零畸变 TUM3 walking，避免把
TUM1/Bonn rectification 差异混入对照；q10/5% 参数不调整。

首轮结果：

| 模式 | 轨迹 | ATE | RPE | FPS |
|---|---:|---:|---:|---:|
| ORB baseline | 816/827 | 0.926133 | 0.078290 | 28.423 |
| semantic-only | 827/827 | 0.019142 | 0.011836 | 28.261 |
| geometry-only | 587/827 | 0.533014 | 0.096267 | 28.506 |
| semantic+geometry | 827/827 | 0.018693 | 0.012298 | 27.083 |

geometry-only 与 ORB baseline 各重复三次，二者 ATE 均约 0.5--0.9 m 且轨迹
覆盖波动。纯几何不能替代 walking 中的人体语义处理。组合模式则保持完整轨迹，
并实际移除 537 个 tracking association、否决 634 个有效深度写图候选，CSV
不变量全部通过。

当前状态修正为：

```text
semantic+geometry sparse frontend   experimental PASS, default OFF
geometry-only standalone            FAIL / diagnostic only
geometry path independent of YOLO   functionally confirmed
stable ATE improvement              not supported
unknown-box GT validation           unresolved
G1-D                                locked
```

详细记录：

- `results/g1_release_2026-07-31/G1_SPARSE_FRONTEND_FOUR_MODE_FREEZE_SPEC.md`
- `results/g1_release_2026-07-31/G1_SPARSE_FRONTEND_FOUR_MODE_RESULT.md`
- `results/g1_release_2026-07-31/G1_WALKING_FOUR_MODE_METRICS.csv`
- `DT-SLAM/tools/run_sparse_frontend_mode.py`

Viewer ON 的 semantic+geometry 也完整处理 827/827 帧，mask age 0，达到轨迹
保存阶段后按既有问题退出 `-11`（shell 245）。该现象与此前 semantic control
一致，本阶段不修改 Viewer/Pangolin。

## 61. Bonn 非遮挡运动箱子检查：实现生效，但箱子贡献未观察到

在 `rgbd_bonn_moving_nonobstructing_box` 上完成 semantic-only 与
semantic+geometry 各三轮正式对照。两种模式全部 778/778，在线 semantic mask
全部同帧。中位结果：

```text
semantic-only       ATE 0.152247  RPE 0.051377  FPS 29.707
semantic+geometry   ATE 0.178114  RPE 0.045061  FPS 29.544
```

组合模式 RPE 中位较低，但 ATE 中位高 16.99%，且逐轮方向不一致，因此没有稳定
定位改善证据。G1-F1/G1-M1 三轮共移除 1,519 个 tracking association、否决
284 个有效深度写图候选，所有 fail-open 与计数不变量通过。

新增默认关闭的 exact removed-association CSV，只在实际清除 MapPoint
association 前记录其像素坐标。Viewer ON 运行完整处理 778 帧并导出 612 点；
轨迹写出后仍复现既有关闭段 `-11`。在 24 帧粗箱框 proxy 中，只有 7 帧有移除，
共 13 点：0 点在粗箱框、0 点在人物 mask、13 点在二者之外。由于框未验证，
这不是 precision/recall；但不能再把几何非零贡献解释成已保护箱子。

当前结论：

```text
G1-F1/G1-M1 code path       usable experimental infrastructure
current unknown-box effect  not demonstrated
q10/5% retuning             forbidden by frozen protocol
G1-D                        locked
```

下一步只在更强遮挡的 `moving_obstructing_box` 做一次判别性开发检查，先空间后
ATE。若仍主要过滤背景，就停止扩张当前 q10 路线并回到文献支持的运动分组/对象
候选设计。

## 62. Bonn 箱子证据漏斗与强遮挡诊断完成

本轮把稀疏几何过滤拆成可核对的逐级证据，没有改变 `q=10`、5% 安全
限制或 SLAM 行为。

非遮挡箱子的粗框中，ORB、LK/深度测量和 quality eligible 数量充足，但
4,564 个 eligible 特征中没有任何 q10 candidate。强遮挡箱子 17 帧中，
粗框内 5,530 个 eligible 产生 26 个 q10，最终只有 2 个在
`SearchLocalPoints()` 后仍有 MapPoint 并被移除，两者都在箱框内。

这是少量真正触及未知箱子的正面证据；但 raw q10 候选在框外的比率为
5.888%，框内仅 0.472%，框外约高 12.48 倍。因此当前单点 residual
不能作为可靠对象判决，不支持降低阈值或开放 G1-D。

当前位置：

```text
G0/G2 evidence infrastructure       complete
G1-F1/G1-M1 sparse protection       experimental, default OFF
unknown-box robust decision         not solved
G1-D dense depth filtering          locked
next method step                    literature-backed motion grouping/object candidate shadow study
```

详细记录：

- `results/g1_bonn_box_2026-07-31/G1_BONN_BOX_EVIDENCE_FUNNEL_AND_OBSTRUCTING_RESULT.md`

## 63. 共同运动分组输入充分性审计完成

本轮没有重复已完成的 balloon/balloon2 轨迹寿命统计，而是核对 Lee 2019
原始运动假设方法，并在 Bonn 箱子上计算连续 residual vector 的多点支持、
方向集中度、组内离散与目标/背景中心分离。

初版混合 moving/stationary 箱框，解释已作废并保留。使用早已冻结的 RGB-only
motion proxy 分层后，非遮挡序列 5 个 moving 帧全部存在多点 coherence，
但同一简单条件也在 10/19 个 stationary 帧触发。moving 与 stationary 的
residual magnitude 中位为 1.682/0.235 px，direction concentration 为
0.975/0.676，centroid separation 为 1.934/0.190 px。

结论是：稀疏两帧输入足以继续研究三维 rigid-motion hypothesis，但当前二维
coherence 无法单独排除静态表面/位姿偏差。下一步只允许写 Lee-style shadow SPEC，
不允许直接聚类、选阈值或放开 G1-D。

详细记录：

- `results/g2_motion_grouping_next_2026-08-01/G2_BOX_TWO_FRAME_MOTION_COHERENCE_INPUT_RESULT.md`

## 64. G2-MH1 稀疏三维刚体运动假设设计冻结

这不是新增总阶段，而是 G2“从连续运动证据形成可靠未知动态判决”内部的下一
个最小工作项。

原始 Lee 2019 方法使用 grid RGB-D scene flow、局部 7 点刚体 hypothesis、
refinement、DBSCAN 和时间模型。本项目只批准第一层适配：复用当前 F1/F3 已有
的两帧稀疏三维对应，为每个有效 anchor 确定性生成一个 7 点局部刚体模型，并
比较该模型与背景相机模型在同一点集上的拟合误差。

已冻结边界：

```text
当前输出                 连续 hypothesis 诊断，不是动态类别
q10                      不参与 hypothesis 输入截断
bbox / person proxy      只用于事后评价，不进入算法
DBSCAN / 时序确认         暂不实现
Tracking/Mapping mutation 无
G1-D                     继续锁定
```

若 moving proxy 内部模型相对背景模型具有重复优势，同时 stationary/真静态不
普遍出现同类结构，才继续研究 hypothesis clustering；否则冻结稀疏局部假设为
负结果。

详细记录：

- `results/g2_motion_grouping_next_2026-08-01/G2_MH1_SPARSE_3D_RIGID_MOTION_HYPOTHESIS_SHADOW_SPEC.md`

## 65. G2-MH1 实现完成，但未放行 hypothesis clustering

G2-MH1 已按 SPEC 实现为默认关闭的 shadow 测量：复用 F1/F3 两帧三维对应，
为每个 anchor 拟合确定性的 7 点局部 SE(3)，同时记录局部与背景模型误差。没有
动态类别、聚类、时序状态、`mvbDynamic` 或 `mvpMapPoints` 修改。

工程测试全部通过，但首轮结果不足以进入下一算法层：

```text
fr1/xyz static：local/background fit median 0.00200 / 0.00543 m
fr1/xyz static：虚假 relative translation/rotation 0.179 m / 0.207 rad
Bonn raw improvement proxy AUC                 0.768
Bonn inside-minus-outside improvement AUC      0.505
Bonn raw/background-to-local RMS ratio AUC     0.800
Bonn inside-minus-outside RMS ratio AUC        0.663
G2-MH1 median runtime                    21--30 ms
full-pipeline FPS                       18.8--20.1
```

这表明局部自由模型会在静态噪声上过拟合；Bonn 中最强信号仍主要来自已有背景
运动不一致。Bonn 本轮还是 geometry-only，5 个 moving proxy 受人物遮挡，所以上述
AUC 只能作开发排序。

当前总计划位置仍在 G2 可靠未知动态判决：G2-MH1 原型已经完成，但对象级运动组
仍未成立。G1-F1/G1-M1 保留为默认关闭的旧实验 baseline，G1-D 继续锁定。下一步
先核对 Lee 的 hypothesis refinement/support verification，而不是直接加 DBSCAN
或经验阈值。

原文复核已确认：7 点在 Lee 方法中只是 seed hypothesis；原方法还会在全部 grid
scene-flow vector 上求误差、扩展支持集并用 `N` 点重估。因此本轮只否定“7 点
训练内拟合可直接判动态”，尚未否定有独立支持验证的完整 motion-hypothesis
思路。任何下一实现都必须另写 support/refinement shadow SPEC。

详细记录：

- `results/g2_motion_grouping_next_2026-08-01/G2_MH1_SPARSE_3D_RIGID_MOTION_HYPOTHESIS_SHADOW_RESULT.md`

## 66. 独立支持验证完成，当前稀疏刚体路线停止

为避免把 7 点训练内过拟合误当作运动证据，本轮使用额外 7 个未参与
拟合的邻近点作 holdout，并在全部非训练点上审计局部/背景模型。

TUM 真静态中训练内 improvement 为 `+1.951 mm`，holdout 变为
`-1.892 mm`，成功揭示过拟合。Bonn moving proxy 上有弱排序差异，但
moving 箱内局部模型仍普遍劣于背景模型，且只有 1--3/7 个 holdout 点
更支持局部模型，不构成独立运动支持集。

按预先冻结规则，停止当前稀疏 7 点刚体 hypothesis 路线，不做支持集重估、
DBSCAN 或新阈值补丁。这不是新总阶段；总计划仍位于“可靠运动组判决”
模块，尚未放行新的 G1-F/G1-M 判决，G1-D 仍锁定。

详细记录：

- `results/motion_group_support_validation_2026-08-01/MOTION_GROUP_SUPPORT_VALIDATION_RESULT.md`

## 67. 当前轻量几何路线完成有限收尾

本轮不新增算法，复用已完成的 TUM/Bonn 正式结果，并补做当前工作树构建、
测试和 30 帧默认/几何回归。

冻结结论：

```text
semantic baseline                  可用，推荐默认主线
F1 + G1-F1 + G1-M1                工程有效，默认关闭的实验模式
semantic+geometry                 可完整运行，无稳定精度改善证据
geometry-only                     仅诊断，不具备独立使用资格
unknown object decision           未完成
G1-D dynamic depth filtering      未实现，不开放
```

当前轻量路线至此收尾，不再继续调 q、邻域或传播补丁。开源 SInDSLAM 作为
后续可选的重型方法升级，需另行做代码、许可证、依赖和接口审计，不算当前
轻量模块的修补。

详细记录：

- `results/geometry_limited_closeout_2026-08-01/GEOMETRY_LIMITED_CLOSEOUT_RESULT.md`
