# DT-SLAM 几何方案负实验与文献来源账本

日期：2026-07-30
工程：`/home/zhu/dynaslam_ws/DT-SLAM`

## 1. 文档目的

本文整理 DT-SLAM 类别无关几何动态检测研究中，已经尝试但效果不足、未达到放行条件，或仅适合作为对照的方法，并追溯其论文依据。

必须先明确：

> 当前实验否定的是我们为 ORB-SLAM2、30 FPS 和轻量前端目标所做的具体适配或组合，不代表原论文方法无效。

原论文往往依赖多关键帧、稠密光流、TSDF、ICP、长期点关联或更完整的区域模型。我们为了保持 ORB-SLAM2 后端不变并控制开销，删除或简化了其中若干条件，因此不能把项目中的负结果表述为“复现某论文失败”。

本文使用以下归属：

- `[L]`：原论文明确提出或实现的内容；
- `[A]`：基于论文方法所做的适配；
- `[S]`：本项目的软件或系统组合设计；
- `[H]`：尚需实验验证的假设或工作阈值。

## 2. 方案、论文来源和实验结论

| 我们尝试的方案              | 主要论文依据                                                                                                                                                                                               | 我们相对论文所做的改造                                                               | 当前结论                                                                                 |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 单参考帧深度 warp 与有符号残差   | Bescos et al., *DynaSLAM: Tracking, Mapping, and Inpainting in Dynamic Scenes*, IEEE RA-L 2018                                                                                                       | `[A/S]` 将多个高重叠历史关键帧和稀疏特征关联深度，改为上一成功帧的稠密 depth forward warp，并加入标准 z-buffer | G0-1/G0-2 测量链可以提供几何不一致证据；它本身没有被判定失败，但证据不足以直接等价为动态判决                                  |
| 全部正残差 seed 的固定阈值区域生长 | DynaSLAM；Palazzolo et al., *ReFusion: 3D Reconstruction in Dynamic Environments for RGB-D Cameras Exploiting Residuals*, IROS 2019                                                                   | `[A/S]` 使用固定 `0.05 m` 四邻域深度连续条件，从全部 positive seed 出发合并其可达区域               | G0-3 出现严重链式传播，region 约覆盖 95%–97% 的有效比较区域，seed 到 region 面积放大约 27–31 倍；G0-3R 支持率修补仍未解决 |
| 深度 K-means＋簇级重投影误差   | Ji et al., *Towards Real-time Semantic RGB-D SLAM in Dynamic Environments*, ICRA 2021                                                                                                                | `[L/A]` 作为 GJ 文献对照接入当前工程，保留“深度聚类—簇级特征重投影—剔除动态簇”的核心结构                      | 固定 K 与真实对象边界不一致，计算开销较高，且区域支持受纹理和匹配影响；保留为论文 baseline，不作为主路线                           |
| 多参考深度一致性             | DynaSLAM                                                                                                                                                                                             | `[A/S]` 改用 ORB-SLAM2 共视参考，并比较全稠密 warp、ORB/grid 稀疏采样和 scale-2 金字塔          | 全稠密成本过高；稀疏采样覆盖不足；scale-2 恢复大部分覆盖，但端到端收益仍不足以稳定满足约 30 FPS 目标                           |
| 深度区域表示与区域证据聚合        | SInDSLAM 的多尺度深度聚类和区域残差思想；Hachiuma et al., *DetectFusion: Detecting and Segmenting Both Known and Unknown Dynamic Objects in Real-time RGB-D Reconstruction*, BMVC 2019                               | `[A/S]` 未实现完整 K-means、区域图、稠密光流、surfel 或 ICP，而是采用轻量深度连通区域，并在区域内聚合多参考证据     | 当前 proxy AUC：walking 0.557、sitting 0.444、fr1/xyz 0.545，接近随机区分；固定深度区域也难以隔离运动箱子        |
| ORB 特征位置的直接深度证据      | DynaSLAM；Ji 2021                                                                                                                                                                                     | `[A/S]` 在当前 ORB 特征中心采样多参考 signed depth residual，避免先依赖完整像素区域               | 在排除人物的箱子条件中，箱内 positive evidence 没有稳定高于背景；说明不足不仅来自区域聚合                               |
| 稀疏自运动补偿残余光流          | Zhang et al., *FlowFusion: Dynamic Dense RGB-D SLAM Based on Optical Flow*, ICRA 2020                                                                                                                | `[A/S]` 将稠密光流改为 ORB 点上的双向 Lucas–Kanade 光流，并通过 RGB-D 和 SE(3) 位姿计算相机诱导光流    | 连续残余量在箱子区域呈现区分趋势，因此该测量没有被判定失败；但单凭它还不能形成低误检的安全二值判决                                    |
| 基于残余光流的硬阈值候选         | FlowFusion；Kalal et al., *Forward-Backward Error: Automatic Detection of Tracking Failures*, ICPR 2010；Li and Lee, *RGB-D SLAM in Dynamic Environments Using Static Point Weighting*, IEEE RA-L 2017 | `[A/S/H]` 融合残余光流、前后向跟踪一致性及鲁棒尺度；`FB ≤ 0.25`、`q ≥ 10` 是本项目选择的工作点，不是论文参数     | 严格留出测试中，箱内候选命中和箱内优于箱外均约为 7/14；完整 MapPoint 候选率约 0.2248%，高于预设 0.20% 上限，因而硬判决未获放行       |
| 两帧局部刚性图和边长应变         | Dai et al., *RGB-D SLAM in Dynamic Environments Using Point Correlations*, IEEE TPAMI 2022；辅以 FlowFusion 的残余运动证据                                                                                     | `[A/H]` 将论文中的长期地图点关联图，简化为两帧 ORB/LK/RGB-D 局部边长变化和跨边关系                      | 当前不是已失败路线：部分指标较乐观，但仍受深度混合、有效节点不足和长边归一化影响，尚不能形成可靠动态判决                                 |

## 3. 各论文与当前工程的准确边界

### 3.1 DynaSLAM

`[L]` DynaSLAM 使用多个高重叠历史关键帧，以特征关联的深度观测进行多视图几何验证，再进行深度区域扩展。

当前 G0/G2 使用的是：

```text
单帧或简化多参考
+ 稠密/金字塔深度 forward warp
+ z-buffer
+ 有符号深度残差
```

因此应写成：

> 受 DynaSLAM 多视图深度一致性启发的轻量适配。

不得写成：

> DynaSLAM 几何模块复现。

另外：

- z-buffer 是标准可见性处理，不是 DynaSLAM 的论文创新；
- 只用正残差作为保守前景进入 seed，是本项目根据遮挡几何作出的 `[S]` 设计；
- 当前 `0.10 m` 等阈值是实验参数，不是 DynaSLAM 参数。

### 3.2 ReFusion

`[L]` ReFusion 的几何残差来自当前 RGB-D 与静态 TSDF 模型的稠密配准，并结合形态学处理、深度感知 flood fill、自由空间和二次配准。

当前工程没有实现：

- TSDF 静态模型；
- TSDF registration residual；
- 自由空间；
- 删除动态区域后的第二次稠密配准。

因此 G0-3 只能称为受 ReFusion 区域传播思想启发的适配。当前 all-seed flood fill 的失败不能表述为 ReFusion 方法失败。

### 3.3 Ji 2021

`[L]` Ji 2021 的主要结构是：

```text
三维深度 K-means
→ 簇内匹配特征平均重投影误差
→ 删除动态簇内特征
```

该方法已作为 GJ 文献 baseline 保留。固定 K、簇与对象不一致、低纹理支持不足和运行成本，是将它作为主方法时的限制，而不是取消其论文对照价值。

### 3.4 DetectFusion 与 SInDSLAM

`[L]` DetectFusion 使用稠密重建模型、几何分割和 ICP 残差发现已知与未知动态对象。SInDSLAM 则提供了多尺度深度处理、深度边界约束和区域内残差统计的依据。

当前工程仅借用了：

- 深度边界感知的区域表示；
- 低分辨率或多尺度近似；
- 区域内证据聚合。

没有复现其完整地图、配准、光流、区域图或重聚类过程。因此 proxy AUC 接近随机，只能否定当前轻量区域组合。

### 3.5 FlowFusion、Kalal 和 Li & Lee

`[L]` FlowFusion 使用实际光流与相机诱导光流之差检测独立运动；Kalal 等人的 forward-backward error 用于识别不可靠跟踪；Li 与 Lee 使用鲁棒静态点权重抑制动态异常。

我们的 F1/F2 是：

```text
稀疏 ORB/LK 观测流
− RGB-D/SE(3) 相机诱导流
→ FB 可靠性过滤
→ 鲁棒归一化
→ 项目自定硬阈值
```

连续残余光流仍有价值。未通过的是项目自定的硬阈值组合，不能写成 FlowFusion、Kalal 或静态权重方法失败。

### 3.6 Dai et al. 点关联方法

`[L]` Dai 等人建立长期地图点相关图，通过相对位置关系的一致性区分静态结构与独立运动。

当前 F3 只使用两帧局部 ORB/LK/RGB-D 边关系，是明显简化的 `[A/H]`。它目前处于 shadow 研究阶段，既没有被证明足以过滤，也没有被判定失败。

## 4. 为什么轻量适配容易失去原论文能力

原论文依赖的条件与当前适配的差异如下：

| 原论文中的能力          | 当前轻量工程中的删减   | 可能后果                 |
| ---------------- | ------------ | -------------------- |
| 多关键帧和历史一致性       | 单参考或有限共视参考   | 对单帧深度噪声和位姿误差更敏感      |
| TSDF/surfel 静态模型 | 相邻帧或关键帧深度    | 缺少长期静态表面和自由空间约束      |
| 稠密 ICP/配准残差      | 深度 warp 残差   | 证据来源更局部，遮挡边界误差更突出    |
| 稠密光流             | ORB 点上的稀疏 LK | 低纹理物体内部覆盖不足          |
| 完整区域图和重聚类        | 连通域或固定深度区域   | 容易跨表面传播，或者把一个对象切碎    |
| 长期点相关图           | 两帧局部边        | 节点不足时统计不稳定，无法利用长期一致性 |

因此当前共同困难不是“几何动态检测方向错误”，而是：

> 在去除 TSDF、稠密光流、长期关联和完整区域模型后，轻量局部证据尚不能同时满足未知动态检出率、低静态误检和约 30 FPS。

## 5. 已经可以冻结的负结论

1. **局部深度连续不等于同一运动对象。**
   从任意正残差 seed 沿固定局部深度差无限传播，会发生严重链式泄漏。

2. **固定深度区域上的简单证据聚合不足以产生通用动态分数。**
   当前 proxy AUC 接近随机，不能选择安全阈值。

3. **直接深度正残差在当前箱子条件下没有稳定的箱内富集。**
   因而不能仅靠特征位置的 signed depth residual 进入真实过滤。

4. **连续残余光流有信号，但硬阈值二值化尚不可靠。**
   F2 留出测试没有达到预设检出率和静态风险要求。

5. **性能与覆盖存在明确矛盾。**
   多参考全稠密计算太慢，简单稀疏采样又丢失覆盖；scale-2 近似只部分缓解。

## 6. 尚不能下的结论

当前证据不能支持以下说法：

- DynaSLAM、ReFusion、Ji 2021、DetectFusion 或 FlowFusion 原方法无效；
- 所有基于深度 warp 的未知动态检测都不可行；
- 所有基于光流的判决都不可行；
- 局部刚性关系已经失败；
- 当前 proxy 候选就是运动箱子真值；
- 几何模块已经可以安全修改 `mvbDynamic`、`mvpMapPoints` 或地图写入。

## 7. 当前最值得保留的正向证据

- G0-1/G0-2 已建立可审计的 depth warp、有效性和有符号残差测量；
- 多参考和坐标域基础设施已经完成；
- 失败的区域传播和固定区域评分已经得到明确排除；
- 稀疏残余光流在箱子区域呈现连续统计差异；
- F3 的局部空间关系在部分指标上表现出区分趋势；
- 全部研究仍保持 shadow-only，没有在证据不足时污染真实 SLAM 状态。

当前更准确的研究位置是：

> 已经完成多条有文献依据的几何证据路线筛选，正在研究如何把连续的残余运动证据与局部空间一致性组合成低误检判决；尚未进入真实几何剔除。

## 8. 参考论文

1. Bescos, B., Fácil, J. M., Civera, J., and Neira, J. *DynaSLAM: Tracking, Mapping, and Inpainting in Dynamic Scenes*. IEEE Robotics and Automation Letters, 2018.
2. Palazzolo, E., Behley, J., Lottes, P., Giguère, P., and Stachniss, C. *ReFusion: 3D Reconstruction in Dynamic Environments for RGB-D Cameras Exploiting Residuals*. IROS, 2019.
3. Ji et al. *Towards Real-time Semantic RGB-D SLAM in Dynamic Environments*. ICRA, 2021.
4. Hachiuma, R., Pirchheim, C., Schmalstieg, D., and Hattori, H. *DetectFusion: Detecting and Segmenting Both Known and Unknown Dynamic Objects in Real-time RGB-D Reconstruction*. BMVC, 2019.
5. Zhang et al. *FlowFusion: Dynamic Dense RGB-D SLAM Based on Optical Flow*. ICRA, 2020.
6. Kalal, Z., Mikolajczyk, K., and Matas, J. *Forward-Backward Error: Automatic Detection of Tracking Failures*. ICPR, 2010.
7. Li, S., and Lee, D. *RGB-D SLAM in Dynamic Environments Using Static Point Weighting*. IEEE Robotics and Automation Letters, 2017.
8. Dai et al. *RGB-D SLAM in Dynamic Environments Using Point Correlations*. IEEE Transactions on Pattern Analysis and Machine Intelligence, 2022.

> 注：SInDSLAM 在本地阶段文档中的书目信息存在不一致。本账本只使用已从本地论文/笔记核对出的技术思想，不在未再次核对原始 PDF 前填写不确定的完整题名和载体。
