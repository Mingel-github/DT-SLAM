# DT-SLAM 当前研究总览与外部评审交流稿

更新时间：2026-07-30
本地权威工作区：`/home/zhu/dynaslam_ws`
核心源码：`/home/zhu/dynaslam_ws/DT-SLAM`
仓库：`github.com/Mingel-github/DT-SLAM`
分支：`main`

---

# 一、本文档用途

本文档用于将当前 DT-SLAM 工程、研究目标、实验进展和未决问题交给 ChatGPT 或
其他研究人员做外部评审。

希望评审者重点回答：

1. 当前类别无关几何路线是否仍值得继续；
2. 是否应放弃轻量区域恢复，转向 feature-level 多帧确认；
3. 在不修改 ORB-SLAM2 后端的约束下，下一项最小且有文献依据的实验是什么；
4. 什么证据足以放行第一次真实 G1-F 几何特征过滤；
5. 如何避免继续增加 shadow 诊断，却长期无法进入真实 ATE 对照。

本文档中的结论尽量区分：

```text
测量事实
代理评价
文献原型
工程改造
待验证假设
```

不能把 proxy、shadow 或方向性结果解释为动态检测 precision/recall。

---

# 二、一句话项目定义

当前目标是：

```text
ORB-SLAM2 RGB-D
+ YOLOv8n-seg 已知动态类别语义分支
+ 类别无关几何运动证据分支
→ 过滤动态 ORB 观测
→ 阻止动态特征和深度写入静态地图
```

论文故事希望保持为：

> 在 ORB-SLAM2 前端进行语义与类别无关几何动态观测过滤，而不是重构整个动态
> SLAM 后端。

---

# 三、研究边界

## 3.1 目标

解决两类污染：

1. 动态 ORB 特征参与相机位姿估计，造成静态背景轨迹和地图错位；
2. 动态对象的特征或深度写入静态地图，造成拖影、残影或重影。

几何模块理想输出：

```text
D_feat：高置信动态 ORB 特征集合
M_depth：动态深度区域
```

其中：

- `D_feat` 用于 tracking 和稀疏 MapPoint；
- `M_depth` 用于未来的稠密点云或深度地图写入。

## 3.2 明确非目标

当前不做：

- 对象级运动轨迹；
- 对象位姿估计；
- 对象级 BA；
- 动态对象地图；
- 修改 g2o 代价函数；
- 重写 Optimizer；
- TSDF、OctoMap、NeRF 或 3DGS；
- 后端动态 SLAM 重构；
- 完整复现 DynaSLAM、ReFusion、StaticFusion、FlowFusion 或 SInDSLAM。

## 3.3 工程限制

- RTX 4060 Ti 可用于 ONNX Runtime CUDA；
- 目标尽量接近 TUM 30 FPS；
- YOLO 保持 `640×640`，不采用检测框降级；
- 当前优先同步语义，async 暂缓；
- 不修改 YOLO 模型和 `YOLOSegment.cc`；
- 不修改 `Optimizer.cc`、g2o、LocalMapping、LoopClosing；
- 不无条件增加第三次 `PoseOptimization`。

---

# 四、术语和证据等级

## 4.1 `[L]/[A]/[S]/[H]`

```text
[L] Literature:
    原论文明确使用的方法或公式。

[A] Adaptation:
    从原论文原理改造到当前 ORB-SLAM2 工程。

[S] System design:
    本项目的软件接口、诊断结构或系统组合。

[H] Hypothesis:
    尚需实验验证的研究假设。
```

## 4.2 Shadow-only

`shadow-only` 表示模块真实运行、真实计时、真实输出 CSV/mask/statistics，但：

```text
不修改 Frame 动态状态
不清除 mvpMapPoints
不改变 PoseOptimization 输入
不阻止 MapPoint 写入
不改变轨迹
```

因此 shadow 阶段的 ATE/RPE 只能说明运行健康，不能说明几何模块改善定位。

## 4.3 Proxy 不等于 GT

当前使用过的代理：

- YOLO person mask；
- 冻结的 RGB-only coarse balloon box；
- agent 基于 RGB temporal clip 给出的 `moving_observable`；
- semantic overlap；
- 静态序列中的背景风险统计。

这些都不是：

- 像素级运动真值；
- 对象精确分割真值；
- feature 动静真值。

---

# 五、原始总计划与当前执行位置

## 5.1 原始总路线

```text
Phase 0:
修复并冻结同步 semantic baseline

G0:
单参考帧 depth warp
+ z-buffer
+ signed depth residual
+ validity/unknown
+ seed / region shadow

G1:
几何动态特征和动态深度真正进入过滤

GJ:
Ji 2021 K-means + cluster reprojection 文献 baseline

G2:
多参考、鲁棒统计、区域表示、短时序确认

Failure-driven:
按失败类型选择稀疏光流、刚性图或自由空间

Final:
baseline / semantic / geometry / semantic+geometry
ATE + RPE + FPS + 地图污染评价
```

## 5.2 当前总体位置

```text
Phase 0 semantic baseline                    已完成并冻结
G0 基础深度证据与失败模式                    已完成
GJ Ji 2021 文献 baseline                     已完成，主方法不采用
G2 多参考、采样、区域和覆盖审计              已完成多轮 shadow
G2-4F1 sparse ego-flow continuous evidence   当前最有希望
G2-4F2 hard feature candidate                strict holdout 失败
G2-4F3/F3U local rigidity                    未形成可靠判决
G2-4F4 simple region context                 未通过表示保真
G1-F real feature filtering                  未开始/锁定
G1-D depth/map filtering                     未开始/锁定
最终 geometry ATE/RPE                        尚无
```

所以不能说项目已经完成 80%。更准确地说：

> 测量基础设施和负结果排除已经较完整，但最关键的“可靠动态判决并安全改善
> SLAM”仍未通过。

---

# 六、Phase 0：同步语义 baseline

## 6.1 已修复内容

- 修复 YOLOv8-seg proto/mask 后处理；
- 统一语义 mask 类型、尺寸和极性；
- dynamic 约定为非零，内部使用 `0=static, 255=dynamic`；
- 修复语义动态特征在匹配、优化准入、关键帧和 MapPoint 创建中的处理；
- CUDA Execution Provider；
- 禁止 CUDA 失败后静默回退 CPU；
- 修复语义线程持锁 sleep 造成的严重阻塞；
- 用条件变量替代 2 ms 轮询；
- 修复 RGB-D 示例只按 Tracking 时间 sleep、遗漏 image IO 的节拍问题；
- 加入端到端计时；
- 保持 Pangolin 可视化可用。

## 6.2 150 帧性能

TUM `fr3_walking_xyz` 前 150 帧：

| 指标                  | 无语义 baseline | 同步语义 CUDA |
| ------------------- | ------------:| ---------:|
| actual FPS          | 29.74        | 29.45     |
| Tracking median     | 12.13 ms     | 10.07 ms  |
| active total median | 20.66 ms     | 32.17 ms  |
| deadline miss       | 0/150        | 54/150    |
| mask age            | —            | 0         |

结论：

- 同步语义接近 30 FPS；
- 但 active total 已接近 33.3 ms，没有稳定几何余量；
- “播放速度接近 30 FPS”不等于每帧都满足 33.3 ms deadline。

## 6.3 正式 TUM ATE/RPE

完整 `fr3_walking_xyz` 827 帧，baseline 与 semantic-only 各三次：

```text
baseline ATE RMSE:
0.940707 / 0.730574 / 0.690009 m

semantic-only ATE RMSE:
0.016477 / 0.017752 / 0.014428 m

baseline RPE RMSE:
0.025407 / 0.024644 / 0.042619 m/frame

semantic-only RPE RMSE:
0.011858 / 0.012771 / 0.011876 m/frame
```

三次中位数：

- ATE：`0.730574 m → 0.016477 m`；
- RPE：下降约 `53.26%`；
- semantic 三次均输出 827/827 poses。

低动态 `fr3_sitting_static` 680 帧各三次：

| 指标              | baseline   | semantic-only |
| --------------- | ----------:| -------------:|
| ATE RMSE median | 0.008022 m | 0.006482 m    |
| translation RPE | 0.005335 m | 0.005492 m    |
| rotation RPE    | 0.163444°  | 0.165418°     |
| actual FPS      | 28.5124    | 28.5243       |

客观结论：

- semantic baseline 已经有明确价值；
- sitting ATE 改善，但 RPE 轻微恶化；
- 不应宣称所有指标都改善；
- Phase 0 可以冻结。

---

# 七、G0：单参考帧深度一致性

## 7.1 已实现

```text
上一成功跟踪帧 CV_32F 米制深度
→ 反投影
→ T_current<-reference
→ forward projection
→ z-buffer
→ predicted depth
→ signed residual
```

定义：

```text
r_z = predicted_depth - current_depth
```

解释：

- `r_z > threshold`：当前表面更近，positive foreground-entry evidence；
- `r_z < -threshold`：当前更远，negative/disocclusion diagnostic；
- 有效小残差：consistent；
- 无投影、无效深度、未覆盖：unknown/no evidence。

方法身份：

> `[A]` 受 DynaSLAM 多视图深度一致性启发的单参考帧 dense 改造，不是
> DynaSLAM geometry reproduction。

## 7.2 坐标与位姿审计

已完成：

- TUM1/TUM3 深度尺度；
- SLAM pose / GT pose 双模式；
- TUM 非零畸变风险；
- Bonn RGB/depth/mask 联合 rectification；
- Bonn `P=K` 共同针孔域；
- Bonn GT frame transform 修正；
- RGB-depth association 文件存在性过滤。

结果显示：

- GT pose 没有系统性消除所有背景 seed；
- 背景 seed 不能全部归因于 SLAM 位姿；
- 深度边缘、遮挡、同步、rasterization 和传感器噪声共同参与。

## 7.3 真静态审计

TUM `fr1/xyz`：

```text
valid comparison coverage mean = 71.68%
absolute residual mean         = 0.00964 m
positive seed ratio mean       = 0.521%
geometry mean                  = 3.48 ms
```

positive seeds：

```text
86.20% 在深度边缘 2 px 内
93.53% 在 3 px 内
98.39% 在 5 px 内
```

说明单参考 dense depth residual 的错误响应高度集中在深度边缘。

## 7.4 G0-3 失败

曾尝试：

```text
所有 positive seed
→ 四邻域局部深度连续 flood fill
```

结果：

- region 覆盖 valid comparison 约 `95%–97%`；
- seed 到 region 面积放大 `27–31×`；
- walking 与 sitting 的 region 统计重叠。

原因：

```text
局部相邻深度连续
≠
属于同一个运动对象
```

墙、地面、斜面和家具通过许多小深度变化形成大型连通分量。该路线已归档为负
baseline，默认关闭。

---

# 八、GJ：Ji 2021 文献 baseline

## 8.1 文献原型

```text
全深度反投影到 3D
→ K-means，N=24
→ ORB/MapPoint 映射到 cluster
→ cluster 内平均重投影误差
→ 高误差 cluster 删除
```

## 8.2 当前复现情况

已完成：

- GJ-1 三维 K-means；
- GJ-2 feature/MapPoint 到 cluster；
- cluster reprojection statistics；
- 自动 proxy 排序审计；
- 论文 PDF、arXiv source 和参数缺失审计。

性能：

```text
K-means geometry ≈ 58 ms/frame
actual FPS ≈ 12.56
```

排序证据：

```text
Top-3 proxy capture        = 36.05%
Top-3 random enrichment    = 2.14×
Top-3 enrichment > 1       = 85/99 frames
```

但：

- walking 与静态 cluster error 重叠；
- Ji 论文没有公开完整 `rho`、绝对阈值和最小支持数；
- 自行设计的相对阈值在静态序列选择约 `50%–54%` 区域；
- GJ-3A 因静态风险失败。

结论：

> 保留为论文 baseline；不作为 30 FPS 主方法，也不进入真实过滤。

---

# 九、G2：多参考深度证据与区域表示

## 9.1 G2-1 多参考 dense

实现：

```text
最多 5 个历史参考
→ 5 次 dense depth warp
→ comparison/positive/negative/consistent vote
```

方向性结果：

```text
C>=1,P>=1 proxy precision ≈34.41%
C>=2,P>=2 proxy precision ≈38.54%
C>=5,P>=2 proxy precision ≈40.46%
```

但 conditional recall 会随票数快速下降。在线同步语义下：

```text
geometry ≈14–19 ms
end-to-end ≈19–21 FPS
```

结论：覆盖较高但太慢，且没有安全阈值。

## 9.2 G2-2R 共视参考

- 使用 ORB-SLAM2 参考关键帧及共视邻居；
- 不用“最近帧”冒充缺失共视参考；
- 发现高共视不等于时间新鲜，参考 age 可达 170–198 帧；
- K=1 仍只有约 25 FPS；
- 单独改变参考选择不足以解决实时性和判决问题。

## 9.3 G2-2S ORB-depth 稀疏采样

受 DynaSLAM feature-associated depth observation 启发：

```text
dense 14–19 ms
→ sparse ORB-depth 0.78–1.00 ms
```

但覆盖：

```text
五参考整图 comparison coverage 仅 0.47%–0.91%
```

结论：

> 计算成功、覆盖失败；保留为 sparse baseline。

## 9.4 G2-3R0 深度区域

受 SInDSLAM relative+absolute depth boundary 启发：

```text
boundary iff
|D(n)-D(p)| > max(0.025 D(p), 0.08 m)
→ 四连通 components
```

它避免了 G0-3 的无限 flood fill，但：

- 大区域仍占 valid depth `45%–75%`；
- TUM3 中约 86% 区域不超过 64 pixels；
- 不是 object segmentation；
- 全分辨率约 3 ms。

## 9.5 G2-3R1/R2/R3/R4

- R1：多参考 evidence 按 region 聚合，信号存在但不足判决；
- R2：full dense coverage 约 `73%–95%`，但成本 `14–19 ms`；
- R3：scale-2 pyramid 保留约 99% region comparison count，候选约
  `4.9–6.1 ms`，但完整同步约 `24–25 FPS`；
- R4：低分辨率 region isolated path 约 `1.2–1.3 ms`，但端到端收益门
  2/3 序列失败，按停止条件终止。

当前不能把任何区域 score 解释为动态分类结果。

---

# 十、G2-4F1：当前最有希望的稀疏运动证据

## 10.1 方法

```text
当前 ORB feature
→ backward LK 到上一成功帧
→ forward-backward quality
→ reference depth + SE(3) 相机诱导 flow
→ observed flow - ego flow
→ continuous residual
```

身份：

> `[A/S]` FlowFusion ego-flow residual 原理的稀疏 ORB/LK + RGB-D/SE(3)
> 改造，不是 FlowFusion 复现。

调用顺序：

```text
初始位姿可用
→ geometry shadow
→ sparse ego-flow shadow
→ TrackLocalMap
```

上一帧参考只在完整 `Track()` 成功后更新；没有增加第三次优化。

## 10.2 成本

F1 通常约：

```text
2.5–2.9 ms/frame
```

曾发现 diagnostic vector 每帧精确 `reserve(size+samples)`，导致近似 O(N²)
历史搬移。删除该错误 reserve 后，static150：

```text
F1 约 2.7–2.9 ms
actual FPS 29.72
deadline miss 0/150
```

这是诊断基础设施修复，不改变 residual。

## 10.3 Development 方向性证据

非 holdout `balloon/balloon2`，冻结的 RGB-only moving proxy、exact C++
person mask：

```text
框内 residual median   = 11.919 px
同帧背景 median         = 0.723 px
paired ratio median     = 20.045×
GT-pose in-box median   = 11.582 px
```

6 个可测 exact-zero-person 帧中均为框内高于背景。

它说明：

> F1 对语义未覆盖的独立运动气球具有连续方向性 evidence。

它没有证明：

- 通用阈值；
- feature precision；
- moving-box 可靠检测；
- 对所有运动方向有效。

---

# 十一、G2-4F2：hard feature candidate 严格留出失败

## 11.1 冻结规则

在打开 `rgbd_bonn_balloon_tracking` 前冻结：

```text
FB <= 0.25 px
frame scale = max(0.001, 1.4826 * median residual)
q = residual / scale
hard candidate iff q >= 10
```

冻结门：

```text
至少 80% moving proxy 帧框内有 candidate
至少 80% 帧框内 candidate rate > 框外
全序列 MapPoint candidate rate <= 0.20%
```

## 11.2 一次性 holdout

14 个 RGB-only moving proxy 均有 exact-zero person mask：

```text
框内有 q>=10 candidate       7/14 = 50%
inside rate > outside        7/14 = 50%
MapPoint candidate rate      0.2248%
```

三个门全部失败。

但连续 residual：

```text
SLAM-pose inside > outside   13/14
GT-pose inside > outside     12/14
inside/outside ratio median  9.98×
```

正确结论：

> 连续 residual 通常局部富集，但当前 whole-frame scale + `q>=10` 的硬候选
> 不稳定。

禁止在该 holdout 上降低阈值修补。

---

# 十二、G2-4F3/F3U：局部刚性增强

## 12.1 F3 两帧局部图

实现：

```text
F1 reliable correspondence
→ current-image Delaunay
→ 两帧 RGB-D 三维 edge length
→ absolute/relative strain
```

来源：

- Dai 等 point-correlation 的局部适配 `[A/H]`；
- 不是其长期 map-point graph optimization 复现。

结果：

```text
in-box flow > outside         15/15
internal strain <= outside    11/14
crossing strain > outside     13/14
F3 runtime median             1.90–1.98 ms
```

方向有一定支持，但 node 支持不足、深度混合和长边分母失真，不能形成判决。

## 12.2 F3U 不确定度归一化

依据：

- Dai edge covariance 原理 `[A]`；
- Khoshelham Kinect `sigma_z ∝ z²` `[L/A]`；
- 3×3 depth mixture 与边长一阶传播 `[S/H]`。

结果：

```text
absolute internal <= background     11/14
uncertainty-normalized internal     4/14
crossing normalized > background   13/14
```

归一化明显没有改善内部刚性可分性。因此：

```text
measurement PASS
default coherence score REJECT
```

被否定的是当前两帧标量简化，不是 Dai 原论文方法。

---

# 十三、G2-4F4：简单区域上下文失败

## 13.1 动机

DetectFusion 和 SInDSLAM 都支持：

```text
motion residual
需要被限制在独立几何 segment/cluster 中解释
```

因此测试：

```text
exact F1 continuous residual
+ G2-3R0 depth components
→ region median/P90/MAD/support
```

## 13.2 结果

17 个冻结粗框帧，14 帧有至少 3 个 selected-region features：

```text
point-level inside > background     14/14
selected region > background        11/14 = 78.57%
预冻结要求                          >=80%
```

典型失败：

```text
balloon frame 39:
  bbox 内 selected features = 6
  selected region total     = 610
  point inside/background   ≈ 20×
  whole region/background   = 0.884×
```

原因：

> 简单 depth-discontinuity component 横跨大块背景，整区 median 稀释了原本
> 很强的点级运动信号。

所以：

- F1 continuous residual 保留；
- G2-3R0 不作为运动对象容器；
- 不修改 region selector 追结果；
- 不进入 online F4；
- 不进入 G1。

---

# 十四、目前哪些结论是乐观的

1. 同步 semantic baseline 已经正确并有显著 walking ATE 改善；
2. CUDA YOLOv8n-seg 可稳定工作；
3. RGB/depth/mask 坐标域和 Bonn rectification 已明确；
4. geometry shadow 数据契约、计时和 no-evidence 建模较完整；
5. F1 sparse ego-flow 对未知气球存在清楚的连续局部富集；
6. F1 成本约 2.5–2.9 ms，仍可能经过调度或非每帧策略接近实时；
7. 负结果均有停止条件，没有把失败包装成成功；
8. 当前没有污染 Optimizer、g2o 或后端，工程回退容易。

---

# 十五、目前哪些结论不乐观

1. 几何尚未真正改变任何 SLAM observation；
2. 当前没有 geometry-only 或 semantic+geometry 的真实 ATE 改善；
3. 单参考 depth seed 高度集中于遮挡/深度边界；
4. flood fill 不能恢复对象；
5. Ji K-means baseline 太慢且静态风险高；
6. 多参考 dense 太慢；
7. ORB-depth sparse 覆盖太低；
8. hard F2 candidate 在 strict holdout 失败；
9. 两帧局部刚性没有提供稳定第二证据；
10. uncertainty normalization 反而恶化内部一致性 proxy；
11. 简单 depth component 会稀释 F1 信号；
12. 同步语义本身几乎用完 30 FPS 预算；
13. 缺少真正独立的 feature/pixel motion GT；
14. 继续增加 shadow 指标有长期不进入 G1 的风险。

总体研究风险应评价为：

```text
semantic 部分：低风险、已成功
geometry measurement：中等风险、已有可用信号
geometry decision/filtering：中高风险、尚未通过
30 FPS full system：中高风险
```

---

# 十六、相对初始计划是否偏离

研究目标没有偏离：

```text
语义 + 类别无关几何
→ 前端动态观测过滤
→ 不改后端
```

但执行路径比最初设想明显更长。原因是最初假定：

```text
depth residual seed
→ region growing
→ dynamic mask
```

能够较快成立。实际实验否定了这个假定，后续必须依次排查：

- 位姿；
- 深度尺度；
- 坐标域；
- seed 质量；
- 多参考覆盖；
- 采样实时性；
- region leakage；
- hard threshold；
- 局部刚性；
- 不确定度。

这些工作属于原计划中 G2 robustness 和 failure-driven enhancement 的展开，不是
另起一个课题。但需要承认：

> 目前 shadow 基础设施投入已经较多，下一阶段必须更直接地服务于第一次安全
> G1-F，而不能继续无限扩展诊断模块。

---

# 十七、当前代码和 Git 状态

## 17.1 本地优先

本项目以本地 `/home/zhu/dynaslam_ws` 为权威。GitHub 用于备份，不要求本地
所有实验产物和大文件都上传。

## 17.2 当前提交

```text
branch      = main
HEAD        = 05fa69b Evaluate sparse-flow holdout and freeze rigidity follow-up
origin/main = b0309ab Add sparse ego-flow shadow and Bonn audits
local ahead = 2 commits
```

最近主要提交：

```text
b8f2a42 Stabilize synchronous CUDA semantic baseline
4d44f62 Document G0-1 validation results
6b57df1 Add geometry diagnostics and Ji reprojection baseline
8fedb2c Add geometry evidence audits and covisibility references
670f008 Add lightweight geometry sampling and region shadows
41ca519 Document geometry region evidence shadow stages
1a4bd8a Add geometry separability and Bonn coordinate audits
b0309ab Add sparse ego-flow shadow and Bonn audits
f964cf0 Freeze reliable sparse-flow candidate gate
05fa69b Evaluate sparse-flow holdout and freeze rigidity follow-up
```

## 17.3 当前尚未提交

F3/F3U/F4 相关修改尚未提交，主要包括：

```text
GeometricDynamicDetector.h/.cc
Tracking.h/.cc
geometric_warp_test.cc
LocalRigidity shadow YAML
audit_local_rigidity_shadow.py
audit_uncertainty_normalized_rigidity.py
audit_region_context_sparse_flow.py
F3/F3U/F4 SPEC/RESULT/HANDOFF/进度文档
```

没有修改：

```text
YOLOSegment.cc
Optimizer.cc
g2o
LocalMapping
LoopClosing
```

当前已验证：

```text
rgbd_tum build PASS
geometric_warp_test PASS
Python audit self-tests PASS
git diff --check PASS
```

---

# 十八、关键数据集和角色

| 数据                     | 用途                                   | 限制                       |
| ---------------------- | ------------------------------------ | ------------------------ |
| TUM fr3/walking_xyz    | 高动态 semantic baseline、geometry proxy | person mask 不是 motion GT |
| TUM fr3/sitting_static | 低动态/混合静动人物                           | “static”指相机运动，不是无人场景     |
| TUM fr1/xyz            | 真静态负对照                               | 相机约25.45 FPS，不是30 FPS    |
| Bonn static_close_far  | 真静态、非零畸变域                            | 必须联合 rectification       |
| Bonn moving box        | F1早期 development                     | RGB temporal 标签有限        |
| Bonn balloon/balloon2  | F1/F3/F4 development                 | coarse bbox 不是 pixel GT  |
| Bonn balloon_tracking  | F2一次性 strict holdout                 | 已打开，不允许回调阈值              |

Bonn archive 中有少量 `depth.txt` 指向不存在的 PNG，association 工具已改为过滤
不存在文件，禁止复用或伪造帧。

---

# 十九、当前性能现实

代表结果：

| 路径                        | 几何成本         | 实际表现                |
| ------------------------- | ------------:| ------------------- |
| semantic-only 150 walking | —            | 29.45 FPS           |
| G0 single depth warp      | 约2–4 ms      | 接近预算边界              |
| G2 dense K=5              | 14–19 ms     | 19–21 FPS           |
| G2 ORB-depth K=5          | 0.78–1.00 ms | 约28.2–28.7 FPS      |
| G2 scale-2 pyramid region | 约6–7 ms含区域   | 约24–25 FPS          |
| F1 sparse ego-flow        | 2.5–2.9 ms   | static150 29.72 FPS |
| F3 local rigidity         | 1.9–2.0 ms   | 额外 shadow 成本        |
| F3U metric 增量             | 0.16–0.20 ms | 不改善可分性              |

Bonn F3U 在线同步 YOLO 完整流程：

```text
balloon   28.40 FPS
balloon2  25.99 FPS
```

这不是全部由 F3U 造成，但说明当前同步完整 pipeline 没有稳定 30 FPS 余量。

---

# 二十、当前最重要的科学判断

当前最有价值的事实不是某个二值 mask，而是：

```text
类别无关 sparse ego-flow continuous residual
在未知气球区域通常明显高于同帧背景。
```

已经失败的，是把这一连续信号转换成可靠动态判决的三种简化：

1. 全帧鲁棒尺度 + hard `q>=10`；
2. 两帧局部 edge rigidity / uncertainty normalization；
3. 简单 depth connected component 的整区聚合。

因此不能说“几何方向完全失败”，也不能说“只差一个阈值”。

更准确地说：

> 运动测量已有信号，但当前缺少既能保留该信号、又能控制静态误删的可靠判决
> 结构。

---

# 二十一、下一步二选一

## 方案 A：更完整的区域路线

参考 SInDSLAM / DetectFusion，增加：

- 3D K-means initial clusters；
- depth gradient + plane/normal edges；
- RAG；
- depth histogram merge；
- residual confined within re-cluster；
- 可能的双阈值和时序先验。

优点：

- 有完整区域上下文；
- 有机会输出 `M_depth`；
- 更适合稠密地图。

风险：

- 工程量明显增大；
- 参数和模块耦合多；
- 很可能突破 30 FPS；
- 接近复现另一套复杂系统；
- 当前论文故事可能从“轻量前端”膨胀为“重型区域分割系统”。

## 方案 B：feature-level 连续证据 + 多帧确认

保留 F1：

```text
continuous residual
+ correspondence quality
+ boundary/invalid risk
+ short temporal persistence
+ conservative feature-level fail-safe
```

暂不要求完整 `M_depth`，先只争取：

```text
D_feat
→ G1-F
→ ATE/RPE/FPS
```

优点：

- 更符合 ORB-SLAM2 稀疏前端；
- F1 已有最强实验证据；
- 计算量较低；
- 不必先解决完整对象分割；
- 可先评价定位贡献。

风险：

- 低纹理箱子上特征不足；
- 不能直接解决稠密深度拖影；
- temporal track 与阈值仍需独立验证；
- 若设计不谨慎，可能只是重新包装 F2 hard gate。

## 当前建议

当前倾向方案 B，理由：

1. 与 30 FPS 和“不改后端”目标更一致；
2. 直接利用已经通过方向性检验的 F1；
3. 能更快进入第一次 G1-F ATE 对照；
4. G1-D 可以在未来另立区域模块，不必阻塞 G1-F。

但方案 B 实施前必须先核对：

- FlowFusion 或相关 sparse residual 的时序一致性依据；
- feature track persistence 的可靠定义；
- Li & Lee 等连续静态权重是否能只用于前端候选，而不改 Optimizer；
- 新 development/validation 数据如何避免复用已打开 holdout；
- fail-safe 如何保证每帧保留足够静态观测。

---

# 二十二、建议给 ChatGPT 的具体问题

请外部评审者不要直接生成代码，先回答：

1. 在 F2 hard gate、F3 rigidity 和 F4 simple region 都失败后，选择
   feature-level 多帧确认是否合理？
2. 有哪些原始论文明确支持：
   - 稀疏 ego-flow residual；
   - feature track 多帧 persistence；
   - 不修改 BA 的前端 hard reject 或 conservative weighting？
3. 如何设计一个不是“凭空修补”的 G2-4F5？
4. 多帧确认应使用：
   - residual 连续累计；
   - 多帧 rank；
   - 两帧/三帧多数；
   - track-level robust statistics；
     中的哪一种？文献依据是什么？
5. 在现有 proxy 不是真值的情况下，第一次 G1-F 放行需要什么最低证据？
6. 是否应该自采一个：
   - 相机固定；
   - 静态箱子；
   - 横向运动箱子；
   - 纵向运动箱子；
   - 无人物；
     的短 RGB-D 数据集，作为独立 feature-level validation？
7. 如何设置 fail-safe：
   - 每帧最大过滤比例；
   - 最小保留 MapPoint 数；
   - 跟踪状态退化时自动关闭 geometry；
   - 仅 MapPoint 写入否决、暂不参与 pose；
     才有文献或工程合理性？
8. 是否应先做 G1-F0：
   - 只模拟“若过滤将删除哪些特征”；
   - 报告剩余内点和姿态可观性；
   - 仍不 mutation；
     再进入真正 G1-F？
9. 从论文贡献角度，当前负结果是否足以支持：
   - “dense depth inconsistency 不适合直接恢复对象”；
   - “稀疏 residual 需要时序而非简单区域扩散”；
     的研究动机？
10. 如果方案 B 也不能建立安全判决，是否应停止类别无关几何过滤，只保留
    semantic baseline + geometry diagnostic 作为工程结果？

---

# 二十三、建议的下一阶段草案

如果外部评审支持方案 B，建议下一阶段暂命名：

```text
G2-4F5:
Temporal sparse ego-flow persistence shadow
```

严格顺序：

1. 先读本地 PaperNotes/PDF；
2. 写 Literature Decision；
3. 写冻结 SPEC；
4. 只实现 track-level continuous statistics；
5. 不产生 dynamic；
6. 用真正静态序列验证 persistent false tail；
7. 用新的、未用于规则选择的 unknown-motion 数据验证；
8. 成功后再写 G1-F0 mutation simulation；
9. G1-F0 通过后，才允许 G1-F；
10. 第一次真实过滤立即跑：
    - baseline；
    - semantic-only；
    - geometry-only；
    - semantic+geometry；
    - ATE/RPE/FPS；
    - retained inliers/MapPoints；
    - tracking lost/relocalization。

如果文献审计无法找到足够依据，或者没有独立验证数据，则不应继续写 F5。

---

# 二十四、主要文档索引

总进度：

```text
results/DT-SLAM_几何模块阶段进度_2026-07-28.md
results/DT-SLAM_HANDOFF_2026-07-29.md
```

语义 baseline：

```text
DT-SLAM/results/DT-SLAM_Phase1_Synchronous_Semantic_Baseline_Report.md
results/ate_semantic_baseline_2026-07-29/REPORT.md
```

F2 strict holdout：

```text
results/g2_4f2_holdout_2026-07-30/
G2_4F2H_STRICT_HOLDOUT_RESULT.md
```

F3：

```text
results/g2_4f3_2026-07-30/
G2_4F3_LOCAL_RIGIDITY_COHERENCE_SHADOW_RESULT.md
```

F3U：

```text
results/g2_4f3u_2026-07-30/
G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_LITERATURE_AUDIT.md
G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_SHADOW_SPEC.md
G2_4F3U_UNCERTAINTY_NORMALIZED_EDGE_SHADOW_RESULT.md
```

F4：

```text
results/g2_4f4_2026-07-30/
G2_4F4_REGION_CONTEXT_LITERATURE_DECISION.md
G2_4F4_REGION_CONTEXT_SHADOW_SPEC.md
G2_4F4_REGION_CONTEXT_SHADOW_RESULT.md
```

负实验与文献来源账本：

```text
DT-SLAM_几何方案负实验与文献来源账本_2026-07-30.md
```

---

# 二十五、希望外部评审保持的原则

1. 不把推测写成事实；
2. 优先本地 PaperNotes 和原始 PDF；
3. 缺失时才使用 Web，并优先原始论文；
4. 每项方法明确 `[L]/[A]/[S]/[H]`；
5. 不将 adaptation 写成 reproduction；
6. 不在已打开 holdout 上调阈值；
7. 不把 semantic proxy 或 coarse bbox 写成 motion GT；
8. 不因实现通过就认为科学假设通过；
9. 不修改 YOLO、Optimizer、g2o 或后端；
10. 不无条件增加第三次 PoseOptimization；
11. 不为了进入 G1 而降低预先冻结的安全条件；
12. 若失败条件触发，应记录负结果并停止；
13. 下一步应服务于第一次安全 G1-F，而不是继续无限增加诊断字段。

---

# 二十六、最简交接结论

```text
1. Semantic baseline 已成功并冻结。

2. Depth-warp、多参考、区域和刚性等 geometry measurement 已大量完成，
   但没有可靠 binary dynamic decision。

3. 当前最强的类别无关证据是 F1 sparse ego-flow continuous residual。

4. F2 hard candidate、F3/F3U rigidity 和 F4 simple region aggregation
   都没有通过预冻结条件。

5. Geometry 尚未真正修改 SLAM，所以当前没有 geometry ATE 改善结论。

6. 下一步建议优先讨论 feature-level temporal persistence + fail-safe，
   而不是继续改 flood fill、edge score 或 simple depth region。

7. 在新方案获得文献依据、独立验证和静态安全证据之前，
   G1-F 与 G1-D 必须继续锁定。
```

---

# 二十七、运动一致性分组方向的最新修正（覆盖“下一步二选一”）

2026-07-30 对外部“feature motion → motion grouping → dynamic object”
建议完成了原始论文和本地代码核对。详细报告：

```text
/home/zhu/dynaslam_ws/
DT-SLAM_运动一致性分组方向批判性审阅_2026-07-30.md
```

最新结论：

1. 接受：F1 continuous sparse ego-flow 是当前最强类别无关证据；
2. 接受：停止 flood fill、hard q、region area 和 simple depth component
   调参；
3. 接受：下一步应研究空间—时间运动一致性分组，而不是直接删除 feature；
4. 修正：“对象级”不是当前必要输出，先寻找 motion-coherent observation
   group；
5. 修正：同一刚体不要求二维 flow vector 相等，而要求对共同运动模型一致；
6. 修正：Dai 前端本来使用两帧，F3 失败不能只归因于帧数；
7. 修正：DetectFusion 的未知动态分支不依赖 YOLO proposal；
8. 修正：当前 YOLO 代码只处理 `person`，不是 person+car，且语义 mask
   表示潜在动态类别，不是真实运动判断；
9. ClusterSLAM、长轨迹 RGB-D segmentation、Jaimez scene flow 都支持
   motion grouping 的学术合理性，但完整方法依赖后端、全序列或重型稠密估计，
   不能直接移植；
10. 当前不实现或命名新的 F5，先做“运动一致性分组的文献与输入可行性审计”。

因此下一项工作不是立即写聚类器，而是先回答：

```text
当前是否有足够稳定的 feature/MapPoint identity 和 2/3/5 帧 track？
未知动态代理区域内是否有足够非语义支持点？
哪一种一致性表达可在不改后端的前提下成立？
能否保留 unknown 状态并控制静态误删？
剩余端到端时间预算是否允许？
```

上述审计通过前：

```text
new grouping implementation    not started
dynamic_decision               none
G1-F / G1-D                    locked
Optimizer / g2o / backend      unchanged
```

---

# 二十八、运动分组输入可行性审计结果

详细报告：

```text
results/g2_4_motion_grouping_2026-07-30/
G2_4_MOTION_GROUPING_INPUT_FEASIBILITY_RESULT.md
```

现有 CSV 没有稳定 `MapPoint::mnId`，当前 F1 也只是当前 ORB 点向上一帧做 LK
回溯，不是多帧 trajectory database。因此使用冻结 development proxy 和原始
Bonn RGB-D，完成了 causal 1/2/3/5-step track survival 审计。

关键结果：

```text
代理内已有 MapPoint：
  balloon   8/76  = 10.53%
  balloon2  4/122 = 3.28%

三张图像中仍有至少 3 个轨迹：
  balloon   4/8 frames
  balloon2  4/9 frames

六张图像中仍有至少 3 个轨迹：
  balloon   2/8 frames
  balloon2  2/9 frames
```

因此：

1. 固定 3/5 帧 persistence 不能作为通用 gate；
2. 基于稳定 MapPoint identity 的长轨迹聚类输入不足；
3. 两帧 transient LK grouping 在多数帧仍可研究，但必须输出
   `unknown/insufficient_support`；
4. sparse grouping 只能争取高置信 `D_feat`，不能替代低纹理物体所需的
   `M_depth`；
5. 用户已放宽 30 FPS 硬约束，更强区域表示应重新纳入比较，而不是仅因计算量
   提前否决。

下一阶段先比较：

```text
Dai-style two-frame point correlation on transient LK nodes
versus
DetectFusion/Tateno-style normal+distance geometric segmentation
```

只选择一条做下一次 shadow representation；G1-F/G1-D 继续锁定。

# 二十九、输入审计后的路线决策：G2-4R1

运动分组输入审计确认：

```text
unknown proxy 内 MapPoint support = 3.28%–10.53%
三图像仍有 >=3 tracks            = 4/8、4/9 frames
六图像仍有 >=3 tracks            = 2/8、2/9 frames
```

所以不能把固定多帧 persistence 或已有 MapPoint long-track grouping 当作通用
下一步。两帧 transient LK grouping 仍有机会，但只能覆盖部分 `D_feat`，不能
解决低纹理和 `M_depth`。

进一步核对原始论文后作出唯一选择：

```text
下一项：
  G2-4R1 Tateno/DetectFusion-style normal+distance
  frame-wise geometric segmentation representation shadow

暂缓：
  Dai-style point-correlation on transient LK nodes
```

选择区域表示不是因为它已经被证明有效，而是：

1. F4 只否定了简单 depth-discontinuity component；
2. normal+distance segment 是尚未测试、且有明确论文表达的更强几何容器；
3. 它能在没有 ORB 纹理时仍处理有效深度；
4. 它为最终 `M_depth` 保留路径；
5. 可先完全离线实现，避免继续扩张已经很大的 `Tracking.cc`。

准确方法身份：

```text
[L/A] Tateno frame-wise normal/distance segmentation adaptation
not Tateno incremental-map reproduction
not DetectFusion reproduction
not dynamic object detection
```

本阶段仍是：

```text
dynamic_decision           = none
direct_slam_state_mutation = none
G1-F / G1-D                = locked
```

详细文档：

```text
results/g2_4_motion_grouping_2026-07-30/
  G2_4_MOTION_GROUPING_INPUT_FEASIBILITY_RESULT.md
  G2_4_MOTION_GROUPING_ROUTE_DECISION.md
  G2_4R1_NORMAL_DISTANCE_SEGMENTATION_SHADOW_SPEC.md
```
